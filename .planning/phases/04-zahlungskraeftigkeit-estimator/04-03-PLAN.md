---
phase: 04-zahlungskraeftigkeit-estimator
plan: 03
type: tdd
wave: 3
depends_on: ["04-02"]
files_modified:
  - lead_analyzer/reasons.py
  - lead_analyzer/pipeline.py
  - tests/test_reasons.py
  - tests/test_pipeline_bedarf.py
autonomous: true
requirements: [ZK-01, ZK-02, ZK-03]
must_haves:
  truths:
    - "reasons.build(verdicts) unchanged when payment=None; build(verdicts, payment) appends '{bedarf} | {payment.reason}'"
    - "Length cap is PER-SECTION (~160 each) so the zahl reason is never truncated away"
    - "analyze_row calls payment.estimate for ALL rows: normal, empty-URL, and exception-boundary"
    - "Empty-URL and exception rows get a real name/branch-based zahl (not placeholder 3)"
    - "bedarf path is UNCHANGED; all prior bedarf tests stay green"
    - "Running the tool end-to-end on sample data emits a real zahl + 'Zahl (Schätzung):' Begründung per row"
  artifacts:
    - path: "lead_analyzer/reasons.py"
      provides: "build(verdicts, payment=None) carrying both rationales, per-section cap"
      contains: "payment=None"
    - path: "lead_analyzer/pipeline.py"
      provides: "analyze_row wired to payment.estimate on all three paths"
      contains: "payment.estimate"
  key_links:
    - from: "lead_analyzer/pipeline.py"
      to: "lead_analyzer.analyzers.payment.estimate"
      via: "call in analyze_row (normal + empty-URL + exception)"
      pattern: "payment\\.estimate"
    - from: "lead_analyzer/pipeline.py"
      to: "lead_analyzer.reasons.build"
      via: "build(verdicts, payment=est)"
      pattern: "reasons\\.build\\(verdicts, payment="
---

<objective>
GREEN + WIRE phase: thread the Zahlungskräftigkeit estimate through `reasons.build` and
`pipeline.analyze_row`, replacing the `_zahl_placeholder` on every code path. After this
plan the tool ranks ideal leads for real: sort by (bedarf, zahl) finally distinguishes
AG firms from Einzelfirmen, and the Begründung column carries both rationales (NACH-01/AC6).

Purpose: Deliver the user-visible outcome of Phase 4 — a meaningful Zahlungskräftigkeit
score on every output row, including edge rows (empty/broken URL), with traceable reasons.
Output: edited reasons.py + pipeline.py + extended test_reasons.py + test_pipeline_bedarf.py.

MVP runnable end-to-end: after this plan `python run.py data/sample_input.xlsx -o out.xlsx`
produces real 1–5 zahl values and "Zahl (Schätzung): …" Begründungen for all rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-zahlungskraeftigkeit-estimator/04-RESEARCH.md

<read_first>
- lead_analyzer/reasons.py — current build(verdicts) + _MAX_LEN=200 cap; extend signature, make cap per-section
- lead_analyzer/pipeline.py — analyze_row: the normal path, the empty-URL early return, and the except-boundary. ALL three currently call _zahl_placeholder
- lead_analyzer/analyzers/payment.py — payment.estimate(record, fr, soup, config) (from Plan 02)
- tests/test_reasons.py — existing build tests (must stay green; payment=None default keeps them unchanged)
- tests/test_pipeline_bedarf.py — existing wiring tests incl. test_zahl_stays_placeholder (this one will CHANGE) and the bedarf direction/boundary tests (must stay green)
</read_first>

<interfaces>
reasons.build(verdicts, payment=None) -> str
  - payment is None  → exactly current behaviour (back-compat)
  - payment given    → f"{bedarf_section} | {payment.reason}", each section capped independently
payment.estimate(record, fr, soup, config) -> PaymentEstimate(zahl, reason, signals)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend reasons.build with optional payment + per-section cap (RED→GREEN)</name>
  <files>tests/test_reasons.py, lead_analyzer/reasons.py</files>
  <behavior>
  - build(verdicts) (no payment) → byte-for-byte unchanged from today (all existing test_reasons tests pass)
  - build(verdicts, payment=PaymentEstimate(4,"Zahl (Schätzung): Rechtsform AG …")) → contains the bedarf summary AND "Zahl (Schätzung): Rechtsform AG"; the two joined by " | "
  - per-section cap: with a very long bedarf body AND a long zahl reason, BOTH the "Bedarf N" summary AND the "Zahl (Schätzung):" prefix survive (zahl not truncated away)
  </behavior>
  <action>
RED first — add to tests/test_reasons.py:
- `test_build_with_payment`: builds 6 verdicts + a PaymentEstimate, asserts the result
  contains both "Bedarf {scoring.bedarf(verdicts)}" and "Zahl (Schätzung):" and " | ".
- `test_build_without_payment_unchanged`: assert build(verdicts) == build(verdicts, None)
  and that no "Zahl" text appears.
- `test_payment_section_not_truncated`: long severe verdicts (like existing test_length_bounded)
  PLUS a long payment.reason → assert "Zahl (Schätzung):" still present in output and the
  bedarf "Bedarf" summary still present (per-section cap, not one global 200 cap that eats zahl).

GREEN — edit reasons.py:
- Change signature to `def build(verdicts, payment=None) -> str:`.
- Keep the existing bedarf computation; treat the existing return value as the `bedarf_section`.
- Replace the single `_MAX_LEN = 200` global cap with a PER-SECTION cap (`_MAX_LEN = 160`)
  applied to the bedarf body independently. When payment is None → return the bedarf section
  exactly as before (truncated to its own cap — keep current behaviour; existing
  test_length_bounded asserts ≤210 so 160 is safe).
- When payment is not None → independently cap the payment.reason to ~160 chars (truncate
  with "…" the same way), then return `f"{bedarf_section} | {payment_section}"`.
- Keep the German docstring; note the per-section cap rationale (zahl reason must survive,
  NACH-01). Keep CITED marker.
Do NOT change the bedarf wording/format — only add the optional second section.
  </action>
  <verify>
    <automated>source .venv/bin/activate && python -m pytest tests/test_reasons.py -q 2>&1 | tail -3</automated>
  </verify>
  <done>All test_reasons tests pass (old + 3 new). build(verdicts)==build(verdicts,None); payment appended with " | "; both sections survive the cap.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire payment.estimate into analyze_row on all three paths (RED→GREEN)</name>
  <files>tests/test_pipeline_bedarf.py, lead_analyzer/pipeline.py</files>
  <behavior>
  - normal path: analyze_row(rec with name "Muster AG", branche "Zahnarzt") → res.zahl is a real estimate (>3, AG+high tier), res.reason contains "Zahl (Schätzung):"
  - empty-URL path: analyze_row(rec(None) with name "Beispiel AG") → bedarf stays 5 "keine Website" AND res.zahl is the AG/branch-based estimate (NOT placeholder 3)
  - exception-boundary path: an analyzer raises → bedarf 5 "Fehler:" AND res.zahl is still a real name/branch estimate (defensive: estimate failure itself falls back to conservative 2, never re-raises)
  - bedarf UNCHANGED: existing bedarf tests still pass; the placeholder-zahl test is REPLACED by a real-zahl assertion
  </behavior>
  <action>
RED first — edit tests/test_pipeline_bedarf.py:
- REPLACE `test_zahl_stays_placeholder` with `test_zahl_is_real_estimate`: rec with
  cells {"Website": url, "Kundenname": "Muster AG", "Branche": "Zahnarzt"}; monkeypatch
  fetch to a modern fetch; assert res.zahl >= 4 and "Zahl (Schätzung):" in res.reason and
  res.zahl != 3-by-coincidence (assert it tracks the estimate: compare to
  payment.estimate(rec, ...).zahl).
- Add `test_empty_url_gets_name_based_zahl`: rec(None) with name "Beispiel AG", branche
  "Treuhand"; spy that fetch is NOT called; assert bedarf==5, reason contains "keine Website",
  AND res.zahl >= 4 (AG + high tier — proves no placeholder 3).
- Add `test_exception_boundary_still_estimates_zahl`: monkeypatch seo.analyze to raise; rec
  with name "Krauer AG", branche "Garage"; assert bedarf==5, reason startswith "Fehler:",
  AND res.zahl is a real estimate (>=3) — the zahl is computed from name/branche which need
  no network.
- Update the `_rec` helper (or add a variant) so tests can pass Kundenname/Branche cells.
- Keep ALL existing bedarf tests intact (six_verdicts_wired, modern→1, dead→5, http-only,
  empty_url bedarf, 403-no-body, reason_is_reasons_build, boundary, direction_gradient).
  Note: test_reason_is_reasons_build currently asserts res.reason == reasons.build(res.verdicts);
  update it to res.reason == reasons.build(res.verdicts, payment=<the est>) OR assert the
  bedarf section matches — keep it a single-source-of-truth check that still holds.

GREEN — edit pipeline.py:
- Add `from .analyzers import ... payment` to imports.
- NORMAL path: after computing `bedarf`, call `est = payment.estimate(record, fr, soup, config)`;
  return RowResult(..., zahl=est.zahl, reason=reasons.build(verdicts, payment=est), verdicts=verdicts).
- EMPTY-URL early return: call `est = payment.estimate(record, None, None, config)` and use
  est.zahl + a reason that keeps "keine Website" for bedarf but ALSO carries the zahl reason.
  Recommended: reason = reasons-style is bedarf-only here (no verdicts), so build a small
  combined string e.g. `f"keine Website | {est.reason}"` so the zahl rationale is visible
  while "keine Website" stays the leading bedarf note. bedarf stays 5.
- EXCEPTION boundary: wrap the estimate defensively so it can NEVER re-raise inside the AC4
  boundary:
  ```python
  try:
      est = payment.estimate(record, None, None, config)
      zahl, zreason = est.zahl, est.reason
  except Exception:
      zahl, zreason = 2, "Zahl (Schätzung): nicht ermittelbar"   # konservativ, AC4
  return RowResult(record.index, bedarf=5, zahl=zahl,
                   reason=f"Fehler: {type(e).__name__} | {zreason}")
  ```
- REMOVE the `_zahl_placeholder` helper (and its use). Keep scoring.placeholder_result
  untouched (Phase-1 tests may still reference it elsewhere — do NOT delete it).
- Update the module + analyze_row docstrings: zahl is now the real payment estimate on all
  three paths; bedarf logic unchanged. German comments, CITED markers.
  </action>
  <verify>
    <automated>source .venv/bin/activate && python -m pytest tests/test_pipeline_bedarf.py -q 2>&1 | tail -3</automated>
  </verify>
  <done>analyze_row emits a real zahl on normal, empty-URL, and exception paths; bedarf path unchanged; _zahl_placeholder removed; all pipeline tests green.</done>
</task>

</tasks>

<verification>
- `python -m pytest -q` → full suite green (135 prior, adjusted for the replaced placeholder test, + payment tests).
- Offline integration (AC10): `python run.py data/sample_input.xlsx -o output/phase4_check.xlsx` → every row has int zahl ∈ [1,5]; AG firms outrank Einzelfirmen on zahl; Begründung shows "Zahl (Schätzung): …" per row; edge rows (empty/broken URL) carry a name/branch zahl, not 3.
- grep gate: `grep -c "payment.estimate" lead_analyzer/pipeline.py` ≥ 3 (all three paths wired).
- grep gate: `grep -c "_zahl_placeholder" lead_analyzer/pipeline.py` == 0 (placeholder removed).
- grep gate: `grep -c "payment=None" lead_analyzer/reasons.py` ≥ 1 (back-compat signature).
</verification>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| analyze_row exception boundary | payment.estimate must never re-raise inside the AC4 per-row net |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | DoS (run abort) | estimate call inside AC4 boundary | mitigate | estimate wrapped in try/except in the exception path → conservative 2, never re-raises |
| T-04-05 | Info Disclosure | empty-URL & error reason strings | accept | reason carries only labelled "Zahl (Schätzung)" notes + error type name; no raw PII |
</threat_model>

<success_criteria>
- reasons.build back-compatible (payment=None) with per-section cap; payment reason never truncated away.
- analyze_row produces a real Zahlungskräftigkeit estimate on ALL three paths; bedarf untouched.
- Tool runs end-to-end offline on sample data with meaningful zahl + traceable Begründung (ZK-01/02/03, NACH-01, AC3/AC5/AC6).
- Full test suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-zahlungskraeftigkeit-estimator/04-03-SUMMARY.md`
</output>
