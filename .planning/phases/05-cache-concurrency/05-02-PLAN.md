---
phase: 05-cache-concurrency
plan: 02
type: tdd
wave: 2
depends_on: [01]
files_modified:
  - tests/test_concurrency.py
  - lead_analyzer/pipeline.py
  - lead_analyzer/cli.py
autonomous: true
requirements: [PERF-03]
must_haves:
  truths:
    - "run() fächert über Zeilen mit ThreadPoolExecutor(max_workers=config.workers) auf."
    - "Threaded-Output (workers=8) ist Zeile-für-Zeile identisch zum sequentiellen (workers=1): gleiche Zeilen, gleicher Sort, gleiche Spalten."
    - "Per-Row-Exception-Isolation bleibt unter dem Pool erhalten: eine kaputte Zeile killt den Lauf nicht (AC4/ROB-03)."
    - "--workers N und --no-cache sind von der CLI in die Config verdrahtet."
    - "Ein zweiter Lauf trifft den Cache: _fetch_network wird nicht erneut aufgerufen (Resume, AC7)."
  artifacts:
    - path: "lead_analyzer/pipeline.py"
      provides: "run() mit ThreadPoolExecutor, index-erhaltende fan-in"
      contains: "ThreadPoolExecutor"
    - path: "lead_analyzer/cli.py"
      provides: "--workers / --no-cache Flags -> Config"
      contains: "--workers"
    - path: "tests/test_concurrency.py"
      provides: "PERF-03 Tests (threaded==sequential, isolation, flags) + PERF-01 Resume"
  key_links:
    - from: "lead_analyzer/cli.py"
      to: "lead_analyzer/config.py"
      via: "args.workers/use_cache -> Config(workers=, use_cache=)"
      pattern: "workers=|use_cache="
    - from: "lead_analyzer/pipeline.py"
      to: "concurrent.futures"
      via: "ThreadPoolExecutor in run()"
      pattern: "ThreadPoolExecutor"
---

<objective>
Ersetze die sequentielle Schleife in run() durch ThreadPoolExecutor(max_workers=
config.workers) mit index-erhaltender fan-in, sodass das Output BYTE-IDENTISCH zum
sequentiellen Lauf bleibt (PERF-03 / AC1). Verdrahte --workers N und --no-cache von
der CLI in die Config. analyze_row bleibt UNVERAENDERT (per-row Boundary intakt).

Purpose: Hunderte Zeilen in vertretbarer Zeit (I/O-bound -> Threads korrekt, stdlib).
Output: ThreadPoolExecutor in run(), CLI-Flags, tests/test_concurrency.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cache-concurrency/05-RESEARCH.md
@.planning/phases/05-cache-concurrency/05-VALIDATION.md
@CLAUDE.md

<read_first>
- lead_analyzer/pipeline.py — run() Zeile ~86 (list comprehension) ist der einzige zu ersetzende Punkt; analyze_row NICHT anfassen
- lead_analyzer/cli.py — build_parser() + Config-Aufbau in main()
- lead_analyzer/config.py — workers: int = 8 und use_cache: bool = True existieren bereits
- lead_analyzer/scoring.py — stable_sort(pairs) bleibt unveraendert (Tiebreaker index garantiert Determinismus)
- tests/conftest.py — make_fetch_result; autouse Netz-Sperre
- 05-01-SUMMARY.md — cache.set_cache_dir und fetch._fetch_network aus Plan 01
</read_first>

<interfaces>
Aus Plan 01 verfuegbar (Wave 1):
```python
# lead_analyzer/cache.py
def set_cache_dir(path) -> None
# lead_analyzer/fetch.py
def fetch(candidates, config) -> FetchResult     # cache-aside
def _fetch_network(candidates, config) -> FetchResult   # reiner HTTP-Pfad (fuer Resume-Spy)
```

Bestehende Pipeline-Signaturen (unveraendert nutzen):
```python
def analyze_row(record: RowRecord, url_col: str, config: Config) -> RowResult  # wirft NIE
def run(config: Config) -> dict
# scoring.stable_sort(pairs) — sort key (-bedarf, -zahl, index)
```

Config-Felder (vorhanden): workers: int = 8, use_cache: bool = True
SAMPLE = data/sample_input.xlsx (Test-Eingabe, CLAUDE.md)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (RED): tests/test_concurrency.py — Determinismus, Isolation, Flags, Resume</name>
  <files>tests/test_concurrency.py</files>
  <behavior>
    - test_threaded_equals_sequential: cache.set_cache_dir(tmp_path); monkeypatch fetch.fetch -> lambda cands,cfg: make_fetch_result(url=cands[0]); run(Config(input=SAMPLE, output=a.xlsx, workers=1)) und run(... workers=8); beide Output-Sheets via table_io.read_rows zeilenweise identisch (gleiche Reihenfolge, gleiche Zellen, gleiche Score-Spalten).
    - test_pool_isolates_bad_row: monkeypatch fetch.fetch so, dass es fuer genau eine URL raise't; run() laeuft durch, alle Zeilen im Output vorhanden (len(out)==len(in)), die kaputte Zeile bekommt Bedarf 5 + "Fehler:"-Reason.
    - test_workers_flag: build_parser().parse_args(["in.xlsx","--workers","3"]).workers == 3; main-Pfad baut Config(workers=3).
    - test_no_cache_flag: parse_args([...,"--no-cache"]) -> use_cache False in der gebauten Config.
    - test_resumability_skips_cached: cache.set_cache_dir(tmp_path); calls=[]; monkeypatch fetch._fetch_network -> append+make_fetch_result; run() zweimal mit identischer Config (use_cache=True); len(calls) nach Lauf 2 == len(calls) nach Lauf 1 (alles Cache-Hits, AC7).
  </behavior>
  <action>
    Erstelle tests/test_concurrency.py. SAMPLE = Pfad zu data/sample_input.xlsx.
    Jeder cache-beruehrende Test ruft cache.set_cache_dir(tmp_path) ZUERST (NIE repo cache/).
    Determinismus-/Isolations-Tests monkeypatchen fetch.fetch (umgeht Netz UND Cache-Schreiben
    fuer make_fetch_result-Fakes); der Resume-Test monkeypatcht fetch._fetch_network (damit der
    echte cache-aside-Pfad aus Plan 01 laeuft und Hits zaehlbar sind).
    Vergleiche Output-Sheets ueber table_io.read_rows(beide Pfade) -> headers gleich, records
    zeilenweise gleich. Imports: pipeline, fetch, cache, table_io, Config, conftest.make_fetch_result.
    Tests MUESSEN initial fehlschlagen (run() noch sequentiell / Flags fehlen) bzw. den
    Determinismus noch nicht garantieren. Deutsche Kommentare, type hints.
    Commit: `test(05-02): add concurrency determinism, isolation, flag + resume tests`.
  </action>
  <verify>
    <automated>cd /Users/nilsseiter/Projekte/MyWEBSITE-Lead-Analyzer && python3 -m pytest tests/test_concurrency.py -q 2>&1 | grep -qE "failed|error|Error" && echo RED-OK</automated>
  </verify>
  <done>tests/test_concurrency.py existiert mit 5 Tests; mindestens die Flag-Tests scheitern (RED), da --workers/--no-cache noch nicht verdrahtet sind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (GREEN): run() auf ThreadPoolExecutor umstellen (index-erhaltend)</name>
  <files>lead_analyzer/pipeline.py</files>
  <action>
    In run() die Zeile `results = [analyze_row(r, url_col, config) for r in records]`
    ersetzen durch index-erhaltende fan-out/fan-in (oben importieren:
    `from concurrent.futures import ThreadPoolExecutor, as_completed`):
    ```python
    results: list[RowResult | None] = [None] * len(records)
    workers = max(1, getattr(config, "workers", 8))
    with ThreadPoolExecutor(max_workers=workers) as pool:
        futs = {pool.submit(analyze_row, r, url_col, config): i
                for i, r in enumerate(records)}
        for fut in as_completed(futs):
            i = futs[fut]
            results[i] = fut.result()        # analyze_row wirft NIE (AC4-Boundary intakt)
    assert all(r is not None for r in results), "Pool hat eine Zeile verloren!"
    ```
    Danach UNVERAENDERT: `pairs = list(zip(records, results))`, `scoring.stable_sort`,
    die len-Invariante, write_output. analyze_row NICHT anfassen. workers=1 == sequentiell.
    Der stabile Sort (Tiebreaker index) macht das Output unabhaengig von der Completion-Reihenfolge.
    Deutsche Kommentare, type hints. Commit: `feat(05-02): parallelize run() with ThreadPoolExecutor`.
  </action>
  <verify>
    <automated>cd /Users/nilsseiter/Projekte/MyWEBSITE-Lead-Analyzer && python3 -m pytest "tests/test_concurrency.py::test_threaded_equals_sequential" "tests/test_concurrency.py::test_pool_isolates_bad_row" "tests/test_concurrency.py::test_resumability_skips_cached" -x -q</automated>
  </verify>
  <done>Threaded-Output == sequentielles Output; kaputte Zeile isoliert; Resume trifft Cache; keine Zeile verloren.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): --workers / --no-cache in cli.py verdrahten</name>
  <files>lead_analyzer/cli.py</files>
  <action>
    In build_parser() zwei Argumente ergaenzen:
    ```python
    p.add_argument("--workers", type=int, default=8,
                   help="Anzahl paralleler Fetch-Threads (Default: 8)")
    p.add_argument("--no-cache", action="store_true",
                   help="Cache komplett umgehen (kein Lesen, kein Schreiben)")
    ```
    In main() den Config-Aufbau erweitern:
    ```python
    config = Config(
        input=args.input,
        output=args.output,
        limit=args.limit,
        write_csv=args.csv,
        reason_column=not args.no_reason,
        workers=args.workers,
        use_cache=not args.no_cache,
    )
    ```
    Default workers=8 entspricht dem Config-Default (Politeness-Annahme A1 aus RESEARCH).
    Deutsche Kommentare. Commit: `feat(05-02): wire --workers and --no-cache CLI flags`.
  </action>
  <verify>
    <automated>cd /Users/nilsseiter/Projekte/MyWEBSITE-Lead-Analyzer && python3 -m pytest tests/test_concurrency.py -q && python3 -m pytest -q</automated>
  </verify>
  <done>Alle test_concurrency.py-Tests gruen; --workers/--no-cache verdrahtet; alle 162 Bestandstests + neue gruen; Output unveraendert vs Phase 4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker-Threads -> geteilte results-Liste | Mehrere Threads schreiben in dieselbe Liste (verschiedene Indizes) |
| Worker-Threads -> cache.put | Gleichzeitige Cache-Schreibzugriffe (in Plan 01 gelockt) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Tampering | results-Liste unter Threads | mitigate | Jeder Future schreibt genau einen festen Index (futs[fut]); keine geteilte mutable Stelle; assert no None |
| T-05-06 | Denial of Service | Pool re-raised eine Row-Exception -> Lauf-Abbruch | mitigate | analyze_row wirft NIE (eigene Boundary); fut.result() kann daher nicht re-raisen; Isolations-Test deckt es ab |
| T-05-07 | Information Disclosure | --workers zu hoch -> rude/rate-limited bei Drittseiten | accept | Default 8 (A1); ein URL pro Host typisch; Politeness-Tradeoff im README (Phase 7) dokumentieren |
</threat_model>

<verification>
- `python3 -m pytest tests/test_concurrency.py -q` — alle gruen
- `python3 -m pytest -q` — 162 Bestand + neue gruen
- `grep -c "ThreadPoolExecutor" lead_analyzer/pipeline.py` >= 1
- `grep -E -c -- "--workers|--no-cache" lead_analyzer/cli.py` == 2 (beide Flags verdrahtet)
- Manuell (Phase-Gate): `python3 run.py data/sample_input.xlsx -o out8.xlsx --workers 8` und `--workers 1` -> identische Sheets
</verification>

<success_criteria>
- run() nutzt ThreadPoolExecutor(max_workers=config.workers), index-erhaltend
- Threaded-Output identisch zum sequentiellen (Determinismus via stable_sort)
- Per-Row-Isolation unter dem Pool erhalten (AC4/ROB-03)
- --workers / --no-cache von CLI in Config verdrahtet
- Resume: zweiter Lauf trifft Cache (AC7); alle 162 Bestandstests gruen; Output unveraendert vs Phase 4
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/05-cache-concurrency/05-02-SUMMARY.md` erstellen.
</output>
