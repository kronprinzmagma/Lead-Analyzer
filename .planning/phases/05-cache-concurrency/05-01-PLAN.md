---
phase: 05-cache-concurrency
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_cache.py
  - lead_analyzer/models.py
  - lead_analyzer/cache.py
  - lead_analyzer/fetch.py
autonomous: true
requirements: [PERF-01]
must_haves:
  truths:
    - "Ein FetchResult überlebt einen Round-Trip durch den Cache verlustfrei (to_dict -> JSON -> from_dict == Original)."
    - "Der Cache-Key ist deterministisch: gleiche normalisierte Kandidaten -> gleicher Key; andere Kandidaten -> anderer Key."
    - "Ein zweiter fetch.fetch()-Aufruf mit denselben Kandidaten trifft den Cache und berührt das Netz NICHT."
    - "Eine korrupte/fehlende Cache-Datei wird als Miss behandelt — kein Crash (AC4)."
    - "schema_version-Mismatch wird als Miss behandelt."
    - "--no-cache (use_cache=False) umgeht Lesen UND Schreiben des Caches komplett."
  artifacts:
    - path: "lead_analyzer/cache.py"
      provides: "key_for/get/put/set_cache_dir — atomarer, thread-sicherer Per-URL-JSON-Cache"
      exports: ["key_for", "get", "put", "set_cache_dir"]
    - path: "lead_analyzer/models.py"
      provides: "FetchResult.to_dict / FetchResult.from_dict"
      contains: "def to_dict"
    - path: "tests/test_cache.py"
      provides: "PERF-01 Unit-Tests (round-trip, key, atomic, corrupt, schema, no-cache)"
  key_links:
    - from: "lead_analyzer/fetch.py"
      to: "lead_analyzer/cache.py"
      via: "cache-aside in fetch.fetch (key_for/get/put)"
      pattern: "cache\\.(key_for|get|put)"
    - from: "lead_analyzer/cache.py"
      to: "FetchResult.to_dict/from_dict"
      via: "JSON payload"
      pattern: "schema_version"
---

<objective>
Baue die transparente Cache-Schicht (PERF-01 / AC7): ein kleiner JSON-File pro URL,
keyed per SHA-256 des normalisierten Kandidaten-Tupels, atomar geschrieben
(temp + os.replace) unter einem Modul-Lock. fetch.fetch() konsultiert den Cache
VOR dem Netz und schreibt bei Miss; analyze_row und das Output bleiben unverändert.

Purpose: Erneuter Lauf nutzt den Cache; ein Abbruch verwirft nicht die ganze Arbeit
(AC7). Gecacht wird das rohe FetchResult — NICHT die Scores — damit spätere
Scoring-Änderungen (Phase 6) keinen Re-Crawl erzwingen.
Output: lead_analyzer/cache.py (neu), FetchResult.to_dict/from_dict, cache-aside
in fetch.fetch, tests/test_cache.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-cache-concurrency/05-RESEARCH.md
@.planning/phases/05-cache-concurrency/05-VALIDATION.md
@CLAUDE.md

<read_first>
- lead_analyzer/models.py — FetchResult-Felder (url, ok, status, final_url, redirected, ssl_ok, headers, html, error); alle JSON-nativ
- lead_analyzer/fetch.py — bestehende fetch(): den HTTP-Körper (Zeilen ~123-185) in _fetch_network extrahieren
- lead_analyzer/config.py — use_cache: bool = True existiert bereits
- tests/conftest.py — autouse Netz-Sperre + make_fetch_result(**overrides); NIE das repo cache/ benutzen
</read_first>

<interfaces>
FetchResult (lead_analyzer/models.py) — alle Felder JSON-nativ:
```python
@dataclass
class FetchResult:
    url: str
    ok: bool
    status: int | None
    final_url: str | None
    redirected: bool
    ssl_ok: bool
    headers: dict
    html: str | None
    error: str | None
```

Aktuelle fetch.py-Signatur:
```python
def fetch(candidates: list[str], config) -> FetchResult: ...  # wirft NIE
def normalize(raw) -> list[str] | None: ...
```

conftest-Helfer:
```python
def make_fetch_result(**overrides) -> FetchResult  # gesunde Defaults, alles überschreibbar
# autouse-Fixture _block_network sperrt requests.Session.get / requests.get
```

Config-Feld (bereits vorhanden): use_cache: bool = True
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (RED): tests/test_cache.py mit failing Tests für Serialisierung, Key und Cache-IO</name>
  <files>tests/test_cache.py</files>
  <behavior>
    - test_round_trip: make_fetch_result(status=403, ok=False, html=None, error="blockiert") -> to_dict -> cache.put -> cache.get -> FetchResult.from_dict == Original (dataclass __eq__).
    - test_key_stability: key_for(["https://x.ch","http://x.ch"]) deterministisch über zwei Aufrufe gleich; key_for(["https://y.ch"]) != key_for(["https://x.ch"]); Key ist 64-stelliger Hex (nur [0-9a-f]).
    - test_corrupt_file_is_a_miss: tmp_path/f"{key}.json" mit "{ kein json" beschreiben -> cache.get(key) is None (kein Raise).
    - test_missing_file_is_a_miss: cache.get(key) für nie geschriebenen Key -> None.
    - test_stale_schema_miss: gültiges JSON mit schema_version=999 schreiben -> cache.get(key) is None.
    - test_atomic_no_tempfile_left: nach cache.put liegt genau eine {key}.json im Verzeichnis und KEINE .tmp-Datei.
    - test_from_dict_tolerates_missing_keys: FetchResult.from_dict({"url":"https://x.ch"}) wirft nicht (Defaults für fehlende Felder).
  </behavior>
  <action>
    Erstelle tests/test_cache.py. Jeder Test ruft ZUERST cache.set_cache_dir(tmp_path) auf
    (NIE das repo cache/ berühren — Pitfall 4). Importiere `from lead_analyzer import cache`,
    `from lead_analyzer.models import FetchResult`, `from conftest import make_fetch_result`.
    Die Tests sind vollständig offline (kein Netz, keine Mocks nötig).
    Diese Tests MÜSSEN initial fehlschlagen (ImportError / AttributeError), da cache.py und
    to_dict/from_dict noch nicht existieren. Deutsche Kommentare, type hints.
    Commit: `test(05-01): add failing cache + serialization tests`.
  </action>
  <verify>
    <automated>cd /Users/nilsseiter/Projekte/MyWEBSITE-Lead-Analyzer && python3 -m pytest tests/test_cache.py -q 2>&1 | grep -qE "error|failed|Error" && echo RED-OK</automated>
  </verify>
  <done>tests/test_cache.py existiert mit 7 Tests; alle scheitern (RED), weil cache.py/to_dict/from_dict fehlen.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1 (GREEN): FetchResult.to_dict/from_dict + lead_analyzer/cache.py</name>
  <files>lead_analyzer/models.py, lead_analyzer/cache.py</files>
  <action>
    1. models.py — an FetchResult zwei Methoden anhängen (oben `from dataclasses import asdict` ergänzen):
    ```python
    def to_dict(self) -> dict:
        # Alle Felder sind JSON-nativ -> asdict ist exakt und wartungsfrei.
        return asdict(self)

    @classmethod
    def from_dict(cls, d: dict) -> "FetchResult":
        # Defensiv: fehlende Keys tolerieren, damit ein schema-Bump nie KeyError wirft.
        return cls(
            url=d.get("url", ""),
            ok=d.get("ok", False),
            status=d.get("status"),
            final_url=d.get("final_url"),
            redirected=d.get("redirected", False),
            ssl_ok=d.get("ssl_ok", False),
            headers=d.get("headers", {}),
            html=d.get("html"),
            error=d.get("error"),
        )
    ```
    2. Neue lead_analyzer/cache.py — exakt diese Shape (stdlib only: hashlib, json, os, tempfile, threading, pathlib):
    ```python
    _LOCK = threading.Lock()
    _CACHE_DIR = Path("cache")
    _SCHEMA_VERSION = 1

    def set_cache_dir(path) -> None:          # Tests rufen das mit tmp_path
        global _CACHE_DIR
        _CACHE_DIR = Path(path)

    def key_for(candidates: list[str]) -> str:
        canon = "\n".join(candidates)         # \n trennt; Hash -> FS-sicherer Name
        return hashlib.sha256(canon.encode("utf-8")).hexdigest()

    def _path(key: str) -> Path:
        return _CACHE_DIR / f"{key}.json"

    def get(key: str):                        # dict | None — wirft NIE (AC4)
        try:
            with _path(key).open("r", encoding="utf-8") as f:
                entry = json.load(f)
        except (FileNotFoundError, json.JSONDecodeError, OSError, ValueError):
            return None                       # Miss ODER korrupt -> Re-Fetch
        if entry.get("schema_version") != _SCHEMA_VERSION:
            return None                       # stale schema -> Miss
        return entry.get("payload")

    def put(key: str, payload: dict) -> None: # atomar + thread-sicher
        entry = {"schema_version": _SCHEMA_VERSION, "payload": payload}
        with _LOCK:
            _CACHE_DIR.mkdir(parents=True, exist_ok=True)
            fd, tmp = tempfile.mkstemp(dir=_CACHE_DIR, suffix=".tmp")
            try:
                with os.fdopen(fd, "w", encoding="utf-8") as f:
                    json.dump(entry, f, ensure_ascii=False)
                os.replace(tmp, _path(key))   # atomic same-filesystem rename
            except BaseException:
                try: os.unlink(tmp)
                except OSError: pass
                raise
    ```
    Deutsche Kommentare, type hints, CITED-Marker für os.replace
    ([CITED: docs.python.org/3/library/os.html#os.replace — same-filesystem atomic]).
    Commit: `feat(05-01): add atomic per-URL cache + FetchResult serialization`.
  </action>
  <verify>
    <automated>cd /Users/nilsseiter/Projekte/MyWEBSITE-Lead-Analyzer && python3 -m pytest tests/test_cache.py -x -q</automated>
  </verify>
  <done>Alle 7 test_cache.py-Tests grün; cache.put hinterlässt keine .tmp-Datei; get tolerant gegen Korruption/Schema-Mismatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): cache-aside in fetch.fetch — _fetch_network extrahieren, Cache davor/danach</name>
  <files>lead_analyzer/fetch.py, tests/test_cache.py</files>
  <behavior>
    - test_fetch_hit_skips_network: cache mit einem put für key_for(cands) vorfüllen; fetch.fetch(cands, Config(use_cache=True)) gibt das gecachte FetchResult zurück OHNE requests (autouse-Netz-Sperre bleibt aktiv und feuert NICHT).
    - test_fetch_miss_then_put: _fetch_network monkeypatchen auf ein make_fetch_result; nach fetch.fetch liegt eine Cache-Datei für key_for(cands); ein zweiter fetch.fetch ruft _fetch_network NICHT erneut auf (Spy-Liste bleibt Länge 1).
    - test_no_cache_flag_bypasses: Config(use_cache=False); _fetch_network-Spy; zwei fetch.fetch-Aufrufe -> Spy Länge 2 UND keine Cache-Datei geschrieben.
  </behavior>
  <action>
    1. fetch.py refaktorieren: den bestehenden HTTP-Körper von fetch() (Session-Aufbau,
       Probing-Schleife, try/finally) wortgleich in eine neue private Funktion
       `def _fetch_network(candidates: list[str], config) -> FetchResult:` verschieben.
       Die HTTP-Logik selbst ÄNDERT SICH NICHT.
    2. fetch() wird zur cache-aside-Hülle (oben `from . import cache` importieren):
    ```python
    def fetch(candidates: list[str], config) -> FetchResult:
        use_cache = getattr(config, "use_cache", True)
        key = None
        if use_cache and candidates:
            key = cache.key_for(candidates)
            cached = cache.get(key)
            if cached is not None:
                return FetchResult.from_dict(cached)   # KEIN Netz, KEINE Session
        fr = _fetch_network(candidates, config)
        if use_cache and key is not None:
            cache.put(key, fr.to_dict())               # atomar, inkrementell
        return fr
    ```
    3. Ergänze die 3 oben spezifizierten Tests in tests/test_cache.py (jeweils
       cache.set_cache_dir(tmp_path) zuerst; _fetch_network via monkeypatch ersetzen;
       Config aus lead_analyzer.config importieren).
    Deutsche Kommentare, type hints. Commit: `feat(05-01): cache-aside inside fetch.fetch`.
  </action>
  <verify>
    <automated>cd /Users/nilsseiter/Projekte/MyWEBSITE-Lead-Analyzer && python3 -m pytest tests/test_cache.py -q && python3 -m pytest -q</automated>
  </verify>
  <done>fetch.fetch trifft bei Hit den Cache ohne Netz; bei Miss wird _fetch_network einmal aufgerufen + Ergebnis gecacht; --no-cache umgeht beides; alle 162 Bestandstests bleiben grün.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Disk cache -> next run | Eine zuvor (evtl. korrupt) geschriebene Cache-Datei wird im Folgelauf gelesen |
| Cache-Key -> Dateipfad | Key fließt in einen Dateinamen unter dem Cache-Dir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | cache.get bei korrupter/truncated JSON-Datei | mitigate | get() fängt JSONDecodeError/OSError/ValueError -> None (Miss + Re-Fetch); put() ist atomar (temp + os.replace), nie truncated |
| T-05-02 | Tampering | cache._path aus Key | mitigate | Key ist sha256-hexdigest (nur [0-9a-f]) -> kein Path-Traversal möglich |
| T-05-03 | Tampering | Gleichzeitiger Same-Key-Write | mitigate | threading.Lock um put(); os.replace atomar; ein File pro URL |
| T-05-04 | Information Disclosure | gescrapte HTML/Header im Cache | accept | cache/ ist in .gitignore (verifiziert); §6 lokal arbeiten; keine vollen HTML-Bodies loggen |
</threat_model>

<verification>
- `python3 -m pytest tests/test_cache.py -q` — alle Cache-Tests grün
- `python3 -m pytest -q` — alle 162 Bestandstests + neue grün
- `grep -rl "cache" tests/ | xargs grep -L "set_cache_dir"` liefert nichts (jeder Cache-Test isoliert tmp_path)
- Keine .tmp-Reste, keine Schreibzugriffe auf repo cache/ während der Tests
</verification>

<success_criteria>
- FetchResult round-trippt verlustfrei durch den Cache (to_dict/from_dict)
- key_for deterministisch, kollisionsfrei, FS-sicher (64 hex)
- fetch.fetch cache-aside: Hit ohne Netz, Miss schreibt atomar, --no-cache umgeht beides
- Korrupte/fehlende Datei + Schema-Mismatch = Miss, nie Crash (AC4)
- Alle 162 Bestandstests bleiben grün (Output unverändert)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/05-cache-concurrency/05-01-SUMMARY.md` erstellen.
</output>
