---
phase: 02-fetch-existence-dim-1-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_fetch.py
  - tests/test_existence.py
  - lead_analyzer/models.py
  - lead_analyzer/fetch.py
  - lead_analyzer/analyzers/__init__.py
  - lead_analyzer/analyzers/existence.py
autonomous: true
requirements: [BED-01]
must_haves:
  truths:
    - "Real network is blocked in the entire test suite by default (autouse conftest)"
    - "normalize() turns a raw cell into an ordered https/www/http variant list, or None for empty/whitespace"
    - "normalize() strips a malformed scheme like 'htp://' and treats the remainder as a bare host"
    - "existence.analyze() returns a Dim-1 verdict: reachable->ok, parked->severe, social->severe, thin->gap, 403/406/429->gap 'blockiert'"
    - "All Phase-1 tests still pass (no regression)"
  artifacts:
    - path: "tests/conftest.py"
      provides: "autouse network-block fixture + fake FetchResult/Response helpers"
      contains: "requests.Session.get"
    - path: "lead_analyzer/models.py"
      provides: "FetchResult dataclass carrier"
      contains: "class FetchResult"
    - path: "lead_analyzer/fetch.py"
      provides: "pure normalize(raw)->list[str]|None"
      contains: "def normalize"
    - path: "lead_analyzer/analyzers/existence.py"
      provides: "pure analyze(fetch_result)->DimensionVerdict"
      contains: "def analyze"
    - path: "lead_analyzer/analyzers/__init__.py"
      provides: "package marker so imports resolve"
  key_links:
    - from: "lead_analyzer/analyzers/existence.py"
      to: "lead_analyzer/models.DimensionVerdict"
      via: "import + return"
      pattern: "DimensionVerdict"
---

<objective>
Build the pure, offline layers of Phase 2 — the URL normalizer, the `FetchResult` carrier, and the Dimension-1 existence verdict — plus the test infrastructure that guarantees the whole suite runs with zero network. No network code ships in this plan; `fetch.py` gets only the pure `normalize()` function (the network `fetch()` lands in plan 02-02).

Purpose: Establish the offline-testable contracts (FetchResult, DimensionVerdict mapping) and the network-block guard that every later test depends on. Interface-first: plan 02-02 wires against the `FetchResult` shape and `existence.analyze()` defined here.
Output: tests/conftest.py, FetchResult on models.py, fetch.normalize(), analyzers/existence.py, and unit tests for normalize + the existence verdict matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fetch-existence-dim-1-robustness/02-RESEARCH.md
@CLAUDE.md

<read_first>
- /Users/nilsseiter/Projekte/MyWEBSITE-Lead-Analyzer/.planning/phases/02-fetch-existence-dim-1-robustness/02-RESEARCH.md — AUTHORITATIVE. Pattern 1 (normalize, lines 147-208), Pattern 3 (existence.analyze + host/marker lists, lines 293-343), the FetchResult dataclass (lines 434-448), and the Validation Architecture test map (lines 476-501). Copy the snippets; they are source-tagged and verified.
- /Users/nilsseiter/Projekte/MyWEBSITE-Lead-Analyzer/lead_analyzer/models.py — extend, do not rewrite. RowRecord, DimensionVerdict, RowResult already exist.
- /Users/nilsseiter/Projekte/MyWEBSITE-Lead-Analyzer/tests/test_phase1_io.py — existing test style (header docstring, `from __future__ import annotations`, openpyxl helper). Match it.
</read_first>

<interfaces>
<!-- Already in lead_analyzer/models.py — use directly, do NOT redefine. -->
```python
@dataclass
class DimensionVerdict:
    dim: int            # 1..6
    level: str          # "ok" | "gap" | "severe"
    reason: str         # human-readable -> log + reason column
    source: str = "html"
```
<!-- FetchResult is NEW — add it to models.py exactly per RESEARCH lines 434-448: -->
```python
@dataclass
class FetchResult:
    url: str
    ok: bool
    status: int | None
    final_url: str | None
    redirected: bool
    ssl_ok: bool
    headers: dict
    html: str | None
    error: str | None
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test scaffold — autouse network block + fakes</name>
  <files>tests/conftest.py</files>
  <action>
Create tests/conftest.py with an autouse, session-or-function-scoped fixture that monkeypatches `requests.Session.get` (and `requests.get`) to raise `AssertionError("network used in tests")` so any un-mocked real request fails loudly — this is the non-negotiable offline guarantee (RESEARCH line 474; CLAUDE.md hard constraint). Provide reusable helpers in the same file:
  - `make_fetch_result(**overrides)` — returns a `lead_analyzer.models.FetchResult` with sensible defaults (ok=True, status=200, html="<html>...</html>", ssl_ok=True, error=None, final_url="https://example.ch/", redirected=False, headers={}), overridable per test.
  - `FakeResponse` — a tiny stand-in exposing `.status_code`, `.url`, `.headers`, `.encoding`, `.apparent_encoding`, and `.iter_content(chunk)` yielding bytes, so plan 02-02's fetch() tests can monkeypatch `Session.get` to return one. Include a way to make `iter_content` yield latin-1/win-1252 bytes for the encoding-fallback test.
Do NOT import or call any real network. Keep helpers importable (module-level functions/classes), not just fixtures, so 02-02 can use them.
  </action>
  <verify>
    <automated>cd /Users/nilsseiter/Projekte/MyWEBSITE-Lead-Analyzer && .venv/bin/python -m pytest -q 2>&1 | tail -5</automated>
  </verify>
  <done>conftest.py exists; existing 15 Phase-1 tests still pass; a throwaway test calling `requests.get("http://example.com")` would raise AssertionError (autouse block active).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FetchResult model + pure normalize() + unit tests</name>
  <files>lead_analyzer/models.py, lead_analyzer/fetch.py, tests/test_fetch.py</files>
  <behavior>
    - normalize(None) -> None ; normalize("") -> None ; normalize("   ") -> None
    - normalize("naehatelier-sutter") with malformed input "htp://naehatelier-sutter" -> strips "htp://", emits candidates for bare host "naehatelier-sutter" (no crash; DNS will fail later -> Bedarf 5)
    - normalize("example.ch") -> ["https://example.ch", "https://www.example.ch", "http://example.ch", "http://www.example.ch"] (order exact)
    - normalize("www.example.ch") -> does NOT add a second www variant (has_www guard); order: https://www.example.ch, http://www.example.ch
    - normalize("https://shop.example.ch/de") preserves path; first candidate is the given scheme+host
    - candidate list is deduped preserving order
  </behavior>
  <action>
Add the `FetchResult` dataclass to lead_analyzer/models.py exactly as in the `<interfaces>` block (RESEARCH lines 434-448) — append it; do not touch RowRecord/DimensionVerdict/RowResult.
Create lead_analyzer/fetch.py with ONLY the pure `normalize(raw) -> list[str] | None` from RESEARCH Pattern 1 (lines 180-207). Follow the normalization order: None/empty/whitespace -> None; strip a non-http(s) `scheme://` prefix; `urlsplit` with "https://" prepended if schemeless; lowercase host; build the ordered https/www/http variants honoring the `has_www` guard; dedupe preserving order. Do NOT add `fetch()` here — that is plan 02-02 (it will append to this same file). Add a module docstring in the existing German-comment style.
Create tests/test_fetch.py covering every case in <behavior> as `test_normalize` (parametrized is fine). Match the Phase-1 test file style.
  </action>
  <verify>
    <automated>cd /Users/nilsseiter/Projekte/MyWEBSITE-Lead-Analyzer && .venv/bin/python -m pytest tests/test_fetch.py::test_normalize -q 2>&1 | tail -8</automated>
  </verify>
  <done>FetchResult importable from lead_analyzer.models; normalize() passes all <behavior> cases; grep `grep -c "def normalize" lead_analyzer/fetch.py` == 1; no `def fetch` yet in fetch.py.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: analyzers/existence.py pure verdict + unit tests</name>
  <files>lead_analyzer/analyzers/__init__.py, lead_analyzer/analyzers/existence.py, tests/test_existence.py</files>
  <behavior>
    - fr with error set AND html None -> DimensionVerdict(1,"severe","nicht erreichbar (...)") [DEAD cause]
    - fr.status in {403,406,429} -> (1,"gap","blockiert – nicht bewertbar") [NOT dead, neutral — must NOT become Bedarf 5]
    - fr.status >= 400 and no html (e.g. 404/500) -> (1,"severe","nicht erreichbar (HTTP {status})") [DEAD]
    - final_url host in PARKED_HOSTS OR parked marker in title/body -> (1,"severe","geparkt/Platzhalter") [DEAD]
    - final_url/input host in SOCIAL_HOSTS (facebook.com etc.) -> (1,"severe","Social-only") [NOT dead, presence exists]
    - reachable 200 with thin body (<300 words) -> (1,"gap","dünner Inhalt")
    - reachable 200 with substantial body -> (1,"ok","erreichbar, Inhalt vorhanden")
  </behavior>
  <action>
Create lead_analyzer/analyzers/__init__.py (empty package marker — required for import resolution, RESEARCH line 460).
Create lead_analyzer/analyzers/existence.py with the pure `analyze(fr) -> DimensionVerdict` from RESEARCH Pattern 3 (lines 316-342). Priority order matters — first match wins, in the exact table order (RESEARCH lines 297-306): (1) error+no html -> dead severe; (2) status in {403,406,429} -> gap "blockiert – nicht bewertbar"; (3) status>=400 + no html -> severe "nicht erreichbar (HTTP ...)"; (4) parked host/marker -> severe "geparkt/Platzhalter"; (5) social host -> severe "Social-only"; (6) thin (<300 words) -> gap "dünner Inhalt"; (7) else ok. Define module-level constants PARKED_HOSTS, PARKED_MARKERS, SOCIAL_HOSTS from RESEARCH lines 307-311. Use `BeautifulSoup(html, "html.parser")`, decompose script/style/nav/footer, get_text for word count. NO network — pure over FetchResult.
Create tests/test_existence.py covering every <behavior> case using the `make_fetch_result` helper from conftest. Explicitly assert that 403 yields level "gap" (not severe) — this is the WAF-block guard (must not be scored Bedarf 5; RESEARCH Pitfall 3, line 387).
  </action>
  <verify>
    <automated>cd /Users/nilsseiter/Projekte/MyWEBSITE-Lead-Analyzer && .venv/bin/python -m pytest tests/test_existence.py -q 2>&1 | tail -8</automated>
  </verify>
  <done>analyze() passes all <behavior> cases incl. the 403->gap guard; `grep -v '^#' lead_analyzer/analyzers/existence.py | grep -c "def analyze"` == 1; analyzers/__init__.py exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| input cell -> normalize() | Untrusted spreadsheet value (may be empty, malformed, non-URL) parsed into candidate URLs. No I/O yet. |
| third-party HTML -> existence.analyze() | Untrusted remote HTML parsed by BeautifulSoup. (Fetched in 02-02; here only the parser is exercised on capped/fixture input.) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | existence.analyze() parsing malformed/large HTML | mitigate | bs4 `html.parser` is pure-Python and bounded; input is the 2 MB-capped body produced in 02-02. Tests feed bounded fixtures. |
| T-02-02 | Tampering | normalize() of malformed scheme (`htp://`) | mitigate | Deterministic string surgery via urllib.parse; malformed prefix stripped, no eval/exec, no shell. Covered by test_normalize. |
| T-02-03 | Information Disclosure | scraped HTML in test fixtures | accept | Fixtures are synthetic; no real third-party data persisted. |
</threat_model>

<verification>
- `cd /Users/nilsseiter/Projekte/MyWEBSITE-Lead-Analyzer && .venv/bin/python -m pytest -q` — full suite green (15 Phase-1 + new normalize + existence tests).
- The autouse conftest block is active: no test performs a real request.
- `grep -c "class FetchResult" lead_analyzer/models.py` == 1.
</verification>

<success_criteria>
- FetchResult dataclass added to models.py without breaking Phase-1 imports.
- normalize() and existence.analyze() are pure (no network) and fully unit-tested offline.
- conftest.py blocks real network for the whole suite and exposes reusable fakes for 02-02.
- All 15 Phase-1 tests still pass.
- 403/406/429 maps to "blockiert" gap, NOT a dead Bedarf-5 cause.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fetch-existence-dim-1-robustness/02-01-SUMMARY.md`.
</output>
