---
phase: 02-fetch-existence-dim-1-robustness
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - lead_analyzer/fetch.py
  - lead_analyzer/scoring.py
  - lead_analyzer/pipeline.py
  - tests/test_pipeline_dim1.py
autonomous: true
requirements: [BED-01, ROB-01, ROB-02, ROB-03]
must_haves:
  truths:
    - "fetch() probes the candidate variants with hard timeout, browser UA, de-CH headers, redirect cap and 2 MB byte cap, and never raises"
    - "An SSLError sets ssl_ok=False and refetches verify=False to still read the body (SSL captured as signal, not a crash)"
    - "latin-1/win-1252 bodies decode with errors='replace' without raising"
    - "Empty URL -> Bedarf 5 'keine Website' with NO network call"
    - "Broken URL / unreachable / timeout / parked -> Bedarf 5 with a note"
    - "403/406/429 -> neutral score, NOT Bedarf 5"
    - "An exception mid-row is isolated by analyze_row's per-row boundary; the run continues"
    - "zahl stays on the Phase-1 placeholder; only the existence-driven part of bedarf is real"
    - "Offline run over data/sample_input.xlsx: len(out)==len(in), all bedarf int 1-5, Kiosk and Nähatelier rows == 5"
  artifacts:
    - path: "lead_analyzer/fetch.py"
      provides: "never-raising fetch(candidates, config) -> FetchResult"
      contains: "def fetch"
    - path: "lead_analyzer/scoring.py"
      provides: "dead->5 override helper for bedarf-from-dim1"
    - path: "lead_analyzer/pipeline.py"
      provides: "real analyze_row with per-row try/except + dead->5"
      contains: "except Exception"
    - path: "tests/test_pipeline_dim1.py"
      provides: "offline ROB-01/02/03 + sample-integration tests"
  key_links:
    - from: "lead_analyzer/pipeline.py"
      to: "lead_analyzer.fetch.fetch"
      via: "call inside analyze_row try-block"
      pattern: "fetch\\.fetch"
    - from: "lead_analyzer/pipeline.py"
      to: "lead_analyzer.analyzers.existence.analyze"
      via: "call on FetchResult"
      pattern: "existence\\.analyze"
    - from: "lead_analyzer/pipeline.py analyze_row"
      to: "Bedarf 5 override"
      via: "dead verdict -> bedarf=5"
      pattern: "bedarf\\s*=\\s*5"
---

<objective>
Add the single network seam `fetch()` to fetch.py and wire the full Dimension-1 slice into the pipeline: empty URL short-circuits to Bedarf 5 with no network; otherwise normalize -> fetch -> existence.analyze -> dead-causes override bedarf to 5, all inside a per-row exception boundary so one bad row never aborts the run. `zahl` stays on the Phase-1 placeholder. After this plan the tool runs end-to-end with a real, never-crashing existence verdict.

Purpose: Deliver the user-visible Phase-2 outcome (real Dim-1 score, robust against the two sample edge cases) on top of the pure layers from 02-01.
Output: fetch.fetch(), a dead->5 scoring helper, the rewritten analyze_row, and the offline ROB-01/02/03 + sample-integration tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-fetch-existence-dim-1-robustness/02-RESEARCH.md
@.planning/phases/02-fetch-existence-dim-1-robustness/02-01-SUMMARY.md
@CLAUDE.md

<read_first>
- /Users/nilsseiter/Projekte/Lead-Analyzer/.planning/phases/02-fetch-existence-dim-1-robustness/02-RESEARCH.md — AUTHORITATIVE. Pattern 2 (fetch() call shape + exception->note map, lines 210-291), Pattern 4 (analyze_row integration + dead set, lines 345-376), Pattern 5 (SSL-as-signal, lines 377-381), the Validation test map (lines 476-501). Copy the snippets verbatim where given.
- /Users/nilsseiter/Projekte/Lead-Analyzer/lead_analyzer/pipeline.py — current analyze_row is a one-line placeholder; replace its body, keep the signature `analyze_row(record, url_col, config)` and the run() orchestration untouched.
- /Users/nilsseiter/Projekte/Lead-Analyzer/lead_analyzer/scoring.py — has clamp_score, placeholder_result, stable_sort. Add a small dead->5 / bedarf-from-dim1 helper; reuse placeholder for `zahl`.
- /Users/nilsseiter/Projekte/Lead-Analyzer/lead_analyzer/config.py — timeout_connect=5.0, timeout_read=10.0 already exist; use them, do not hardcode.
- tests/conftest.py (from 02-01) — use `make_fetch_result`, `FakeResponse` helpers; the autouse network block is already active.
</read_first>

<interfaces>
<!-- Defined in 02-01 — consume directly, do not redefine. -->
From lead_analyzer/models.py:
```python
@dataclass
class FetchResult:
    url: str; ok: bool; status: int | None; final_url: str | None
    redirected: bool; ssl_ok: bool; headers: dict; html: str | None; error: str | None
```
From lead_analyzer/fetch.py:  `def normalize(raw) -> list[str] | None`
From lead_analyzer/analyzers/existence.py:  `def analyze(fr: FetchResult) -> DimensionVerdict`
From lead_analyzer/scoring.py:  `clamp_score(value) -> int`, `placeholder_result(record) -> RowResult`, `stable_sort(pairs)`
From lead_analyzer/models.py:  `RowResult(index, bedarf, zahl, reason="", verdicts=[])`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Never-raising fetch() network seam</name>
  <files>lead_analyzer/fetch.py</files>
  <behavior>
    - Clean 200 (mocked Session.get returns FakeResponse) -> FetchResult(ok=True, status=200, html=<decoded body>, ssl_ok=True, final_url set, error=None)
    - request shape: Session.get called with timeout=(config.timeout_connect, config.timeout_read), browser User-Agent, Accept-Language de-CH, allow_redirects=True, stream=True; session.max_redirects low cap (10). Assert via captured kwargs on the mocked get.
    - SSLError on verify=True -> retried with verify=False; body still read; ssl_ok=False; no raise
    - Timeout -> moves to next candidate; all candidates Timeout -> FetchResult(ok=False, html=None, error="Timeout"-ish)
    - ConnectionError on all variants -> FetchResult(ok=False, html=None, error="nicht erreichbar")
    - latin-1/win-1252 bytes -> decode with errors='replace', no UnicodeDecodeError
    - body capped at ~2 MB (iter_content stops after the cap)
    - fetch() NEVER raises for any mocked exception, incl. a bare unexpected Exception
  </behavior>
  <action>
APPEND `fetch(candidates, config) -> FetchResult` to lead_analyzer/fetch.py using RESEARCH Pattern 2 (lines 239-291) verbatim, plus the `_read_capped` helper (lines 250-257) and the `_HEADERS`/`_MAX_BYTES` module constants (lines 241-248). Key knobs (RESEARCH lines 214-224): `timeout=(config.timeout_connect, config.timeout_read)`; browser UA + `Accept-Language: de-CH,de;q=0.9,en;q=0.5` + Accept header; `session.max_redirects = 10`; `stream=True` + 2 MB cap via iter_content; decode declared->apparent encoding with `errors="replace"`; `verify=True` first, on SSLError set ssl_ok=False and refetch `verify=False`. Exception->note map per RESEARCH lines 225-233 in the given order of specificity (SSLError, Timeout, TooManyRedirects, RequestException, bare Exception). On a real HTTP response (any status) STOP probing further variants (RESEARCH line 167). Suppress only the scoped InsecureRequestWarning on the verify=False path (RESEARCH line 381). Do NOT add a Session-level retry/backoff, thread pool, or cache (Phase 5/6 — locked out).
Write the fetch() tests in tests/test_fetch.py (same file as 02-01's normalize tests) named per the RESEARCH test map: test_request_shape, test_ssl_signal, test_timeout, test_encoding_fallback, plus a clean-200 happy path. Monkeypatch `requests.Session.get` to return conftest's FakeResponse or raise the mapped exceptions.
  </action>
  <verify>
    <automated>cd /Users/nilsseiter/Projekte/Lead-Analyzer && .venv/bin/python -m pytest tests/test_fetch.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>All <behavior> cases pass; `grep -v '^#' lead_analyzer/fetch.py | grep -c "def fetch"` == 1; fetch() catches every mocked exception and returns a FetchResult (never raises); timeout uses config fields (grep `timeout_connect` in fetch.py).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire analyze_row — dead->5 override, keep zahl placeholder, per-row boundary</name>
  <files>lead_analyzer/scoring.py, lead_analyzer/pipeline.py</files>
  <behavior>
    - empty URL (cell None/"") -> RowResult(bedarf=5, reason="keine Website"), NO fetch.fetch call (assert fetch not invoked)
    - dead verdict (reason starts "nicht erreichbar" or "geparkt") -> bedarf=5
    - social-only / thin (severe-not-dead / gap) -> high-but-not-5 provisional bedarf (severe-not-dead=4, gap/ok=3), NOT overridden to 5
    - 403 verdict ("blockiert") -> neutral provisional (3), NOT 5
    - existence.analyze raising mid-row -> analyze_row returns degraded RowResult(bedarf=5, reason starts "Fehler:"), no exception escapes
    - zahl always equals the Phase-1 placeholder value (do not compute real Zahlungskräftigkeit)
    - verdicts list carries the Dim-1 DimensionVerdict for the log (NACH foundation)
  </behavior>
  <action>
Add a small helper to lead_analyzer/scoring.py: `bedarf_from_dim1(verdict) -> int` implementing the provisional mapping from RESEARCH Pattern 4 (lines 360-366): dead causes (reason starts with "nicht erreichbar" or "geparkt") -> 5; severe-not-dead (e.g. Social-only) -> 4; gap/ok -> 3; wrap in clamp_score. Keep clamp_score, placeholder_result, stable_sort unchanged. Reuse placeholder_result's zahl (constant 3) for `zahl` — expose it however is cleanest (e.g. a `_zahl_placeholder()` returning 3, or read placeholder_result(record).zahl). Do NOT implement real Zahlungskräftigkeit.
Rewrite the BODY of `analyze_row(record, url_col, config)` in lead_analyzer/pipeline.py per RESEARCH Pattern 4 (lines 351-371), keeping the existing signature and run() untouched:
  1. wrap everything in `try: ... except Exception as e:` returning a degraded RowResult(bedarf=5, zahl=placeholder, reason=f"Fehler: {type(e).__name__}") — bare Exception (RESEARCH Anti-pattern line 385, Pitfall 1).
  2. `raw = record.cells.get(url_col)`; `candidates = fetch.normalize(raw)`; if None -> return RowResult(bedarf=5, zahl=placeholder, reason="keine Website") with NO fetch.
  3. else `fr = fetch.fetch(candidates, config)`; `v = existence.analyze(fr)`; `bedarf = scoring.bedarf_from_dim1(v)`; return RowResult(index, bedarf, zahl=placeholder, reason=v.reason, verdicts=[v]).
Import fetch and existence at module top: `from . import scoring, table_io, fetch` and `from .analyzers import existence`.
  </action>
  <verify>
    <automated>cd /Users/nilsseiter/Projekte/Lead-Analyzer && .venv/bin/python -m pytest tests/test_pipeline_dim1.py -q 2>&1 | tail -10</automated>
  </verify>
  <done>analyze_row signature unchanged; `grep -c "except Exception" lead_analyzer/pipeline.py` >= 1; empty-URL path makes no fetch call; dead->bedarf 5; 403->not 5; zahl == placeholder; verdicts carries the Dim-1 verdict.</done>
</task>

<task type="auto">
  <name>Task 3: Offline ROB tests + sample-integration test</name>
  <files>tests/test_pipeline_dim1.py</files>
  <action>
Create/complete tests/test_pipeline_dim1.py with the offline tests from the RESEARCH test map (lines 481-488), monkeypatching `lead_analyzer.fetch.fetch` (or `requests.Session.get`) — NEVER real network:
  - test_empty_url: cell None -> RowResult bedarf 5 "keine Website", and assert fetch.fetch was NOT called.
  - test_broken_url: "htp://naehatelier-sutter" with fetch mocked to return an unreachable FetchResult -> bedarf 5, note startswith "nicht erreichbar".
  - test_block_not_dead: 403 FetchResult -> bedarf NOT 5 (neutral), level gap "blockiert".
  - test_row_boundary: monkeypatch existence.analyze (or fetch.fetch) to raise -> analyze_row returns degraded RowResult bedarf 5 "Fehler:...", does not raise; (optionally drive through pipeline.run on a 2-row fake to prove the loop continues).
  - test_sample_offline: run the full `pipeline.run` over data/sample_input.xlsx with `fetch.fetch` monkeypatched to a deterministic fake (e.g. a reachable FetchResult for non-empty URLs). Assert: `len(out) == len(in)`; every bedarf is int in [1,5]; the Kiosk (empty URL) row and the Nähatelier (broken URL -> mock returns unreachable) row both score bedarf 5. Read the output via openpyxl like test_phase1_io.py. Use config.limit if needed to keep it fast, but ensure both edge-case rows are included.
Match the Phase-1 test file style (docstring, `from __future__ import annotations`, openpyxl helper).
  </action>
  <verify>
    <automated>cd /Users/nilsseiter/Projekte/Lead-Analyzer && .venv/bin/python -m pytest -q 2>&1 | tail -12</automated>
  </verify>
  <done>Full suite green (15 Phase-1 + all Phase-2 tests); test_sample_offline asserts len(out)==len(in), bedarf int 1-5, both edge-case rows == 5; no test touches the real network (autouse block intact).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (this tool) -> third-party site | Outbound HTTP to untrusted SME sites; response body and headers are untrusted input. |
| spreadsheet cell -> fetch() | Untrusted URL value drives the request target. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Denial of Service | fetch() reading remote body | mitigate | `stream=True` + 2 MB byte cap via iter_content; hard `timeout=(5,10)` from config; bounds an endless/huge response (RESEARCH line 519). |
| T-02-05 | Denial of Service | redirect loop | mitigate | `session.max_redirects = 10`; TooManyRedirects caught -> note, next variant, no crash (RESEARCH line 520). |
| T-02-06 | Tampering / DoS | one bad row aborting the batch | mitigate | bare `except Exception` per-row boundary in analyze_row returns a degraded RowResult; run() loop continues (ROB-03; RESEARCH Pitfall 1). |
| T-02-07 | Cryptography misuse | global verify=False masking TLS failures | mitigate | verify=True first; verify=False ONLY as a scoped fallback after recording ssl_ok=False; InsecureRequestWarning suppressed only on that path (RESEARCH line 381, V6). |
| T-02-08 | Information Disclosure | scraped third-party data leaking to git | accept | Phase 2 persists no scraped data; output/.env gitignore is Phase 7 scope. |
</threat_model>

<verification>
- `cd /Users/nilsseiter/Projekte/Lead-Analyzer && .venv/bin/python -m pytest -q` — full suite green (15 Phase-1 + Phase-2 fetch/existence/pipeline tests).
- Offline guarantee intact: the autouse conftest network block from 02-01 is still active; no test performs a real request.
- Sample integration: `pytest tests/test_pipeline_dim1.py::test_sample_offline -q` proves len(out)==len(in), bedarf int 1-5, and the Kiosk + Nähatelier rows score 5.
- fetch() never raises (covered by mocked-exception cases); analyze_row never raises (per-row boundary).
- 403/406/429 does not become Bedarf 5.
</verification>

<success_criteria>
- Real, never-crashing Dimension-1 verdict wired into analyze_row; dead causes override bedarf to 5.
- Empty URL scores Bedarf 5 "keine Website" with zero network.
- The two sample edge cases (Kiosk empty URL, Nähatelier broken URL) both score 5 in the offline integration test.
- zahl unchanged from the Phase-1 placeholder; no thread pool / cache added.
- Tool and full test suite run with no network; all 15 Phase-1 tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-fetch-existence-dim-1-robustness/02-02-SUMMARY.md`.
</output>
