---
phase: 07-hardening-readme-full-sample-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [SETUP-01]
must_haves:
  truths:
    - "A new user can read README.md and run the tool in under 5 minutes"
    - "README states the tool runs fully WITHOUT any API keys (graceful degradation)"
    - "README explains the one command, all CLI flags, the .env section, and where output lands"
    - "README explains both scores, the 6 Bedarf dimensions, and the Zahlungskräftigkeit signals"
  artifacts:
    - path: "README.md"
      provides: "Setup-and-run documentation per AC9/SETUP-01"
      contains: "pip install -r requirements.txt"
    - path: "README.md"
      provides: "Optional API-key documentation"
      contains: "PAGESPEED_API_KEY"
  key_links:
    - from: "README.md"
      to: "run.py"
      via: "documented one command"
      pattern: "python run.py data/sample_input.xlsx"
---

<objective>
Write the root README.md so a new user goes from clone to a scored output file in
under 5 minutes, with zero API keys, per AC9 / SETUP-01.

Purpose: SETUP-01 is part of the Definition of Done (CLAUDE.md §7). The tool is
already complete and offline-capable; this plan documents how to set it up and run it.
Output: README.md at the repository root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@CLAUDE.md
@requirements.txt
@run.py
@lead_analyzer/cli.py
@.env.example
@docs/scoring_website_bedarf.md

<facts>
<!-- Verified during planning; the executor should use these directly, no re-discovery needed. -->
- Entry command: `python run.py data/sample_input.xlsx -o output/leads.xlsx`
- Install: `python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt`
- Runtime deps (requirements.txt): openpyxl>=3.1, requests>=2.31, beautifulsoup4>=4.12
- Tests: `python -m pytest` — 202 tests green (pytest is dev-only, not in requirements.txt runtime block)
- CLI flags (lead_analyzer/cli.py): positional `input`; `-o/--output` (default output/leads_scored.xlsx);
  `-n/--limit N`; `--csv`; `--no-reason`; `--workers N` (default 8); `--no-cache`; `--no-pagespeed`
- .env keys (.env.example): PAGESPEED_API_KEY, OPENAI_API_KEY, ANTHROPIC_API_KEY — ALL optional.
  Without PAGESPEED_API_KEY the PageSpeed/Dim-3 tier is OFF and Dim 3 uses the viewport heuristic (graceful degradation).
- Real full run: 42 rows in 3.7s with no key (PSI off), fully offline-capable.
- Output column "Begründung" (reason) is on by default; `--no-reason` drops it.
- Output is sorted descending by Website-Bedarf, then Zahlungskräftigkeit (ideal leads on top).
- 6 Bedarf dimensions (docs/scoring_website_bedarf.md): 1 Existenz & Substanz, 2 Technische Basis (HTTPS/SSL, eigene Domain),
  3 Mobile & Performance (Viewport + optional PageSpeed), 4 Auffindbarkeit/SEO, 5 KI-/Answer-Engine-Bereitschaft, 6 Inhalt/Aktualität/Conversion.
- Zahlungskräftigkeit signals: legal form from company name (AG/GmbH/Einzelfirma), branch kaufkraft tier,
  website size signals (multiple locations/team/careers) — labelled as estimate.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README.md — setup, command, flags, .env</name>
  <files>README.md</files>
  <action>
Create README.md at the repository root. Keep it skimmable (headings + short lists,
no walls of prose). Required sections, in this order:

1. **Title + one-paragraph "what it does"**: Excel/CSV of customers in -> same table out
   plus two integer columns `Website-Bedarf (1-5)` and `Zahlungskräftigkeit (1-5)`,
   sorted descending by Bedarf then Zahlungskräftigkeit so the ideal leads (high need + high
   purchasing power) sit on top.
2. **Setup (under 5 minutes)** — the exact commands, copy-pasteable:
   - `python -m venv .venv`
   - `source .venv/bin/activate`  (note Windows: `.venv\Scripts\activate`)
   - `pip install -r requirements.txt`
3. **Run it** — the ONE command verbatim: `python run.py data/sample_input.xlsx -o output/leads.xlsx`
   State where the output lands (the `-o` path; default `output/leads_scored.xlsx`).
4. **CLI flags** — a table or list documenting EVERY flag from lead_analyzer/cli.py:
   `input`, `-o/--output`, `-n/--limit`, `--csv`, `--no-reason`, `--workers` (default 8),
   `--no-cache`, `--no-pagespeed`. One short line each.
5. **API keys / .env (all optional)** — explain: copy `.env.example` to `.env`, add keys if you have them.
   State explicitly that the tool **runs fully WITHOUT any keys** (graceful degradation): without
   `PAGESPEED_API_KEY` the PageSpeed/Dim-3 tier is simply off and Dim 3 falls back to the viewport
   heuristic. Keys only enhance, never required. Mention `PAGESPEED_API_KEY` by name.
6. **Re-running / cache** — results are cached per URL on disk; a re-run skips already-analyzed
   URLs and an interrupted run is not lost. Use `--no-cache` to bypass.
7. **Tests** — `python -m pytest` (202 tests). Note pytest is dev-only.

Do NOT document any feature that does not exist (CLAUDE.md §6 scope). Pull all command
strings verbatim from the <facts> block — do not invent flags or paths.
  </action>
  <verify>
    <automated>test -f README.md && grep -q 'pip install -r requirements.txt' README.md && grep -q 'PAGESPEED_API_KEY' README.md && grep -q 'python run.py data/sample_input.xlsx' README.md && grep -q -- '--no-pagespeed' README.md && grep -q -- '--no-cache' README.md && grep -q -- '--workers' README.md && grep -q 'python -m pytest' README.md && echo OK</automated>
  </verify>
  <done>
README.md exists with: install command, the one run command, all 8 CLI flag tokens,
the PAGESPEED_API_KEY/.env section stating no-keys operation, cache re-run note, and pytest line.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the two-scores + dimensions explainer to README</name>
  <files>README.md</files>
  <action>
Append a "How the scores work" section to README.md (skimmable):
- **Website-Bedarf (1-5)** — higher = more need = better lead. Derived from the SIX dimensions
  (list all six by name: Existenz & Substanz; Technische Basis; Mobile & Performance; Auffindbarkeit/SEO;
  KI-/Answer-Engine-Bereitschaft; Inhalt/Aktualität/Conversion). One line each on what is measured.
  Note "no reachable website -> 5" override. Link to docs/scoring_website_bedarf.md for the full rubric.
- **Zahlungskräftigkeit (1-5)** — higher = more purchasing power. Estimated from public signals:
  legal form from the company name (AG/GmbH/Einzelfirma), branch kaufkraft tier, and website size
  signals (multiple locations/team/careers). State clearly it is a labelled **estimate**, not audited finance data.
- Mention the `Begründung` reason column (on by default, `--no-reason` to drop) shows which signals drove each score.
Keep it tight — this is a reference, not an essay. Do not duplicate the full rubric; link to it.
  </action>
  <verify>
    <automated>grep -q 'Website-Bedarf' README.md && grep -q 'Zahlungskräftigkeit' README.md && grep -q 'scoring_website_bedarf.md' README.md && grep -qi 'estimate\|Schätzung' README.md && echo OK</automated>
  </verify>
  <done>
README has a scores section naming both scores, listing the 6 Bedarf dimensions, describing the
Zahlungskräftigkeit signals as an estimate, and linking the scoring rubric doc.
  </done>
</task>

</tasks>

<verification>
- README.md renders as valid Markdown.
- Every CLI flag in lead_analyzer/cli.py appears in README.
- README states the tool runs without keys.
</verification>

<success_criteria>
A new user can clone, follow README setup, and produce output/leads.xlsx in under 5 minutes
with zero API keys (SETUP-01 / AC9).
</success_criteria>

<output>
After completion, create `.planning/phases/07-hardening-readme-full-sample-run/07-01-SUMMARY.md`
</output>
