---
phase: 07-hardening-readme-full-sample-run
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - .env.example
autonomous: true
requirements: [SETUP-03]
must_haves:
  truths:
    - ".env, output/, and cache/ are gitignored and not committed"
    - ".env.example is the only env file committed"
    - "No firm/personal data leaves the local machine via git"
  artifacts:
    - path: ".gitignore"
      provides: "Ignore rules for secrets and generated data"
      contains: ".env"
    - path: ".env.example"
      provides: "Committed template with empty key placeholders"
      contains: "PAGESPEED_API_KEY"
  key_links:
    - from: ".gitignore"
      to: "output/"
      via: "ignore rule"
      pattern: "output/"
---

<objective>
Verify and lock the privacy/scope hardening: .env, output/, and cache/ are gitignored and
not tracked; .env.example is the only committed env file; no firm/personal data is committed
(SETUP-03 / CLAUDE.md §6).

Purpose: §6 requires the tool to work locally without leaking firm/personal data. This plan
proves nothing sensitive is tracked and ensures the safe template is committed.
Output: confirmed .gitignore coverage; .env.example committed.

NOTE: No new features. This is verification + git hygiene only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@CLAUDE.md
@.gitignore
@.env.example

<facts>
<!-- Verified during planning. -->
- .gitignore already covers: .env, __pycache__/, *.pyc, .venv/, venv/, node_modules/, outputs/, output/, *.cache, .cache/, cache/, .pytest_cache/, data/output*.xlsx
- `git ls-files` shows NO tracked .env, output/, or cache/ entries (good).
- .env.example exists in the working tree but is NOT yet tracked by git — it must be committed (it is the safe template).
- No actual .env file exists in the working tree (good).
- .env.example contents: PAGESPEED_API_KEY=, OPENAI_API_KEY=, ANTHROPIC_API_KEY= (all empty placeholders).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit git tracking + ensure .env.example is committed</name>
  <files>.gitignore, .env.example</files>
  <action>
1. Confirm nothing sensitive is tracked:
   `git ls-files | grep -E '^(\.env$|output/|cache/|\.cache/)'` MUST return nothing.
   If it returns anything, untrack it with `git rm --cached <path>` (do NOT delete the working file).
2. Confirm .gitignore covers `.env`, `output/`, and `cache/`. They already exist per <facts>;
   if any is missing, add it. Do not remove existing rules.
3. Verify .env.example contains only EMPTY placeholders (no real secrets). It must contain
   `PAGESPEED_API_KEY` with no value after the `=`. If any key has a value, blank it.
4. Stage .env.example so it becomes the one committed env file: `git add .env.example`.
   (The actual commit happens in the phase commit step; just ensure it is staged/tracked-ready.)
5. Confirm no real `.env` file exists in the working tree; if one does, do NOT stage or commit it.
  </action>
  <verify>
    <automated>! git ls-files | grep -Eq '^(\.env$|output/|cache/|\.cache/)' && grep -q '^\.env$' .gitignore && grep -q 'output/' .gitignore && grep -q 'cache/' .gitignore && grep -q 'PAGESPEED_API_KEY=$' .env.example && echo OK</automated>
  </verify>
  <done>
No .env/output/cache tracked; .gitignore covers all three; .env.example has only empty
placeholders and is staged for commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Record the privacy check note</name>
  <files>.gitignore</files>
  <action>
Add a short comment block at the top of .gitignore documenting WHY these rules exist
(CLAUDE.md §6: tool works locally, never commit secrets or firm/personal data — .env holds
keys, output/ and cache/ hold scored firm data). Keep existing rules intact; only prepend
comment lines (start with `#`). This makes the privacy intent self-documenting for future edits.
  </action>
  <verify>
    <automated>head -3 .gitignore | grep -q '^#' && grep -q '^\.env$' .gitignore && grep -q 'output/' .gitignore && echo OK</automated>
  </verify>
  <done>
.gitignore has an explanatory comment header and still contains all secret/data ignore rules.
  </done>
</task>

</tasks>

<verification>
- `git ls-files` lists no secrets or generated data.
- .env.example is the only env file ready to commit.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo -> git remote | Secrets (.env) and scraped firm/personal data (output/, cache/) must never cross into version control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information Disclosure | .env API keys | mitigate | .gitignore `.env`; only .env.example (empty placeholders) committed; audit `git ls-files` confirms no .env tracked |
| T-07-02 | Information Disclosure | output/ + cache/ (scraped firm data) | mitigate | .gitignore `output/`, `cache/`, `.cache/`; audit confirms none tracked (CLAUDE.md §6 local-only) |
| T-07-03 | Information Disclosure | accidental real .env created later | accept | Documented gitignore comment header warns future edits; low risk for a local solo CLI tool |
</threat_model>

<success_criteria>
.env, output/, and cache/ are gitignored and untracked; .env.example is the only committed
env file; no firm/personal data is committed (SETUP-03 / §6).
</success_criteria>

<output>
After completion, create `.planning/phases/07-hardening-readme-full-sample-run/07-03-SUMMARY.md`
</output>
