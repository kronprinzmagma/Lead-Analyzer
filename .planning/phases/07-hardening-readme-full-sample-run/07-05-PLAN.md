---
phase: 07-hardening-readme-full-sample-run
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - lead_analyzer/analyzers/existence.py
  - tests/test_existence.py
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-03]
must_haves:
  truths:
    - "REQUIREMENTS.md traceability reflects reality: ZK-01/02/03 are Done, Phase 7 rows are Done"
    - "ROADMAP.md progress table matches the actual completed phases"
    - "The unreachable Begründung no longer reads 'nicht erreichbar (nicht erreichbar)'"
    - "All 202 tests still pass after the optional polish"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate requirement traceability table"
      contains: "ZK-01"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate phase progress table"
      contains: "Complete"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: "ROADMAP.md"
      via: "consistent phase status"
      pattern: "Done"
---

<objective>
Planning-state bookkeeping plus one optional, low-risk text polish:
(1) fix REQUIREMENTS.md traceability — Phase 4 (ZK-01/02/03) is complete but reads "Pending";
unify status wording; mark SETUP-01/02/03 Done once this phase ships;
(2) correct the ROADMAP.md progress table to match the phases that are actually complete;
(3) OPTIONAL/LOW — de-duplicate the "nicht erreichbar (nicht erreichbar)" reason text in
existence.py without breaking any of the 202 tests.

Purpose: closes the housekeeping debt called out for Phase 7 and removes a cosmetic Begründung
wart. No runtime behaviour or scope change beyond the cosmetic reason string (CLAUDE.md §6).
Output: corrected REQUIREMENTS.md + ROADMAP.md; optionally a cleaner unreachable reason string.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/REQUIREMENTS.md
@lead_analyzer/analyzers/existence.py

<facts>
<!-- Verified during planning. -->
- REQUIREMENTS.md traceability currently lists ZK-01/ZK-02/ZK-03 as "Pending" but Phase 4 is built and tested.
- Mixed status vocabulary in REQUIREMENTS.md: some rows say "Done", others "Complete". Unify to one word ("Done").
- SETUP-01/02/03 read "Pending"; they become Done at the end of THIS phase (Plans 01-04).
- ROADMAP.md progress table is stale: Phases 1-4 and 6 show "Not started" / "0/TBD" though their plans exist and (per repo state) are built; Phase 5 correctly shows Complete.
- existence.py line 79: `f"nicht erreichbar ({fr.error or 'kein Body'})"`. When fr.error == "nicht erreichbar"
  (set in fetch.py lines 158/175), this renders the doubled "nicht erreichbar (nicht erreichbar)".
- Tests for existence live in tests/test_existence.py (read it before editing the reason string).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix REQUIREMENTS.md + ROADMAP.md traceability</name>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <action>
In .planning/REQUIREMENTS.md:
- In the v1 checklist, tick ZK-01, ZK-02, ZK-03 ([ ] -> [x]) and SETUP-01, SETUP-02, SETUP-03 ([ ] -> [x]).
- In the Traceability table, set ZK-01/ZK-02/ZK-03 status to "Done" and SETUP-01/SETUP-02/SETUP-03 status to "Done".
- Unify the status vocabulary: replace every "Complete" cell in that table with "Done" so the column reads one word.
- Do NOT change Phase numbers or requirement IDs; only statuses/checkboxes.

In .planning/ROADMAP.md:
- Tick the phase header checkboxes for Phases 2, 3, 4, 6, 7 that are done ([ ] -> [x]) consistent with repo reality
  (Phase 5 already ticked). Phase 7 becomes [x] only as part of this phase's completion.
- Update the "## Progress" table: set Phases 1-7 to their real "Plans Complete" counts and Status "Complete"
  (use the plan counts already listed under each Phase Detail; Phase 7 = 5 plans). Fill Completed = 2026-06-14.
- Mark each Phase 7 plan checkbox in the Phase 7 detail list and refresh the plan list to the 5 plans
  (07-01 README, 07-02 sample_run_rationale, 07-03 privacy, 07-04 verification, 07-05 bookkeeping+polish).

Keep edits surgical — status/checkbox/count only, no prose rewrites.
  </action>
  <verify>
    <automated>grep -q '| ZK-01 | Phase 4 | Done |' .planning/REQUIREMENTS.md; grep -q '| SETUP-01 | Phase 7 | Done |' .planning/REQUIREMENTS.md; ! grep -q '| Complete |' .planning/REQUIREMENTS.md; grep -q '07-05' .planning/ROADMAP.md</automated>
  </verify>
  <done>REQUIREMENTS.md traceability shows ZK-* and SETUP-* as Done with unified vocabulary; ROADMAP
progress table and Phase 7 plan list match reality (5 plans).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OPTIONAL/LOW — de-duplicate the unreachable reason text</name>
  <read_first>lead_analyzer/analyzers/existence.py, tests/test_existence.py, lead_analyzer/fetch.py</read_first>
  <behavior>
    - When fr.error == "nicht erreichbar" and there is no body, the verdict reason is exactly
      "nicht erreichbar" (NOT "nicht erreichbar (nicht erreichbar)").
    - When fr.error is a distinct message (e.g. "timeout"), the reason still reads "nicht erreichbar (timeout)".
    - When fr.error is empty, the reason still reads "nicht erreichbar (kein Body)".
    - The dead=True / severe / Bedarf-5 behaviour is UNCHANGED.
  </behavior>
  <action>
This task is OPTIONAL and LOW priority. If touching it risks ANY of the 202 tests, SKIP it and note
the skip in the SUMMARY — do not force it.

In lead_analyzer/analyzers/existence.py around line 79, change the reason builder so it does not
double the phrase: if `fr.error` already equals (or starts with) "nicht erreichbar", emit just
"nicht erreichbar"; otherwise keep the `f"nicht erreichbar ({fr.error or 'kein Body'})"` form.

First read tests/test_existence.py: if an existing test asserts the doubled string, update that
assertion to the de-duplicated string (the test was asserting a wart). Add a one-line test covering
the fr.error == "nicht erreichbar" case if none exists. Do not weaken any other assertion.
Run the full suite afterward; it MUST stay at 202 passing.
  </action>
  <verify>
    <automated>python -m pytest tests/test_existence.py -q 2>&1 | tail -1 | grep -qE 'passed'; ! python -c "from lead_analyzer.analyzers import existence" 2>&1 | grep -q Error</automated>
  </verify>
  <done>Unreachable reason no longer doubles "nicht erreichbar"; test_existence.py green; full suite
still 202 (or task cleanly skipped with a note if it risked the suite).</done>
</task>

</tasks>

<verification>
- REQUIREMENTS.md and ROADMAP.md statuses are internally consistent and match repo reality.
- Optional polish either landed (suite still 202) or was skipped with a documented reason.
</verification>

<success_criteria>
Planning state is accurate (ZK + SETUP requirements Done, ROADMAP progress correct) and the
cosmetic unreachable-reason duplication is fixed without regressing the 202-test suite.
</success_criteria>

<output>
After completion, create `.planning/phases/07-hardening-readme-full-sample-run/07-05-SUMMARY.md`
</output>
