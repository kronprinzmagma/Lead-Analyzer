---
phase: 07-hardening-readme-full-sample-run
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/sample_run_rationale.md
autonomous: true
requirements: [SETUP-02]
must_haves:
  truths:
    - "A reader can see the score distribution over the full 42-row sample"
    - "The reader understands WHY the example scores are sensible, including edge cases"
    - "The empty-URL and broken-URL edge cases are explained (both Bedarf 5)"
    - "The big-vs-small firm contrast is explained (AG/Treuhand high Zahl vs Coiffeur low)"
  artifacts:
    - path: "docs/sample_run_rationale.md"
      provides: "AC10 rationale: distribution + edge-case + firm-size justification"
      contains: "Kiosk"
  key_links:
    - from: "docs/sample_run_rationale.md"
      to: "data/sample_input.xlsx"
      via: "documented full-run results"
      pattern: "42"
---

<objective>
Write the AC10 rationale doc: run the tool over all 42 rows of data/sample_input.xlsx,
record the score distribution, and explain in writing why the example scores make sense —
explicitly covering the two edge cases and the big-vs-small firm contrast (SETUP-02 / AC10).

Purpose: AC10 / Definition of Done requires a written justification that the sample scores
are plausible. This makes the tool's judgement auditable.
Output: docs/sample_run_rationale.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@CLAUDE.md
@docs/scoring_website_bedarf.md

<facts>
<!-- REAL data from a live 42-row run (no key, PSI off, 3.7s). Cite these numbers verbatim. -->
- Run command: python run.py data/sample_input.xlsx -o output/leads.xlsx (no PAGESPEED_API_KEY -> PSI off, offline-capable).
- Website-Bedarf distribution: 1:3, 2:12, 3:15, 4:10, 5:2
- Zahlungskraeftigkeit distribution: 1:3, 2:9, 3:10, 4:7, 5:13
- Edge case A — "Kiosk am Lindenplatz" (empty URL): Bedarf 5 "keine Website"; Zahlungskraeftigkeit 1 (Detailhandel/Kiosk low tier).
- Edge case B — "Nähatelier Sutter" (broken htp:// URL): Bedarf 5 "nicht erreichbar / ungueltiges SSL"; Zahlungskraeftigkeit 2 (Handwerk mid tier).
- Ideal leads (top): "KMU Treuhandexperte GmbH" B4/Z5; "Lippuner Immobilien & Verwaltungen AG" B4/Z5; "Oerlikon Zahnarzt" B4/Z4.
- Lowest need: "ONE! Treuhand" B1/Z4; "Coiffure Heidi" B1/Z1 (all "alle Dimensionen ok -> Bedarf 1").
- Big-vs-small contrast: AG/Treuhand-type firms reach Z4-Z5; a one-person Coiffeur reaches Z1.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce the full 42-row run</name>
  <read_first>CLAUDE.md (AC10), docs/scoring_website_bedarf.md</read_first>
  <files>docs/sample_run_rationale.md</files>
  <action>
Run the full sample to confirm reproducibility (offline / no key):
  python run.py data/sample_input.xlsx -o output/leads.xlsx
Confirm it processes 42 rows and writes the output without error. Read output/leads.xlsx with
openpyxl and count each score 1-5 in both score columns to capture the actual distributions.

If the live distributions differ from the numbers in <facts>, use the LIVE numbers you observe
and note the difference — do not fabricate. Do NOT commit output/leads.xlsx (it is gitignored).
Begin docs/sample_run_rationale.md with a "## Lauf" section: the exact command, "42 Zeilen verarbeitet",
and that it runs offline without keys.
  </action>
  <verify>
    <automated>test -f output/leads.xlsx; test -f docs/sample_run_rationale.md; grep -q 'python run.py data/sample_input.xlsx' docs/sample_run_rationale.md; grep -q '42' docs/sample_run_rationale.md</automated>
  </verify>
  <done>Full run completed over 42 rows; doc started with the reproducible command and row count.</done>
</task>

<task type="auto">
  <name>Task 2: Write the distribution + rationale (edge cases + firm size)</name>
  <read_first>docs/sample_run_rationale.md (the ## Lauf section from Task 1)</read_first>
  <files>docs/sample_run_rationale.md</files>
  <action>
Complete docs/sample_run_rationale.md with three sections:

1. "## Verteilung" — a table of the Website-Bedarf distribution (1:3, 2:12, 3:15, 4:10, 5:2) and
   the Zahlungskraeftigkeit distribution (1:3, 2:9, 3:10, 4:7, 5:13). One sentence noting the
   spread is plausible (most sites land mid-range; few are perfect, few are absent/broken).

2. "## Edge-Cases" — explain why each is sensible:
   - "Kiosk am Lindenplatz" (empty URL) -> Bedarf 5 ("keine Website" override) + Zahlungskraeftigkeit 1
     (small Detailhandel kiosk). Tie the Bedarf 5 to the "no reachable website -> 5" rule.
   - "Nähatelier Sutter" (broken htp:// URL) -> Bedarf 5 ("nicht erreichbar / ungueltiges SSL") +
     Zahlungskraeftigkeit 2 (small Handwerk). Show robustness: a broken URL did not crash, it scored sensibly.
   Use the exact names "Kiosk" and "Nähatelier" so they appear verbatim.

3. "## Gross vs. klein (Zahlungskraeftigkeit)" — contrast the firm-size signal:
   - High: "KMU Treuhandexperte GmbH" B4/Z5, "Lippuner Immobilien & Verwaltungen AG" B4/Z5 — legal form
     (GmbH/AG) + strong branch tier drive Z4-Z5; these are the ideal leads (top of sort).
   - Low: "Coiffure Heidi" B1/Z1 — one-person Coiffeur, modern site (all dimensions ok -> Bedarf 1),
     a poor lead despite a working site.
   - Note the "lowest need" cluster (ONE! Treuhand B1/Z4) where a good site correctly yields Bedarf 1.
   Close with one sentence: the default sort (Bedarf desc, then Zahlungskraeftigkeit desc) puts the
   ideal high-need + high-power leads on top (CLAUDE.md §3).

Cite the numbers from <facts> verbatim (use live numbers from Task 1 if they differ).
  </action>
  <verify>
    <automated>grep -q 'Kiosk' docs/sample_run_rationale.md; grep -q 'Nähatelier' docs/sample_run_rationale.md; grep -q 'Coiffure Heidi' docs/sample_run_rationale.md; grep -qE 'Treuhandexperte|Lippuner' docs/sample_run_rationale.md; grep -q '15' docs/sample_run_rationale.md; grep -q '13' docs/sample_run_rationale.md</automated>
  </verify>
  <done>Doc documents both distributions, explains both edge cases (each Bedarf 5), and contrasts
high-Zahl AG/Treuhand firms against the low-Zahl Coiffeur, citing the real numbers.</done>
</task>

</tasks>

<verification>
- docs/sample_run_rationale.md cites the real distributions and both edge cases.
- output/leads.xlsx produced and NOT committed (gitignored).
</verification>

<success_criteria>
A reader can verify the sample scores are plausible from the rationale doc, including the
empty-URL, broken-URL, and big-vs-small firm cases (SETUP-02 / AC10).
</success_criteria>

<output>
After completion, create `.planning/phases/07-hardening-readme-full-sample-run/07-02-SUMMARY.md`
</output>
