---
phase: 07-hardening-readme-full-sample-run
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03, 05]
files_modified: []
autonomous: false
requirements: [SETUP-01, SETUP-02, SETUP-03]
must_haves:
  truths:
    - "The full 42-row sample run is reproducible end-to-end with no keys"
    - "All 202 tests pass"
    - "README, rationale doc, and privacy hardening are all present and correct"
  artifacts:
    - path: "README.md"
      provides: "Setup docs (from Plan 01)"
      contains: "pip install -r requirements.txt"
    - path: "docs/sample_run_rationale.md"
      provides: "AC10 rationale (from Plan 02)"
      contains: "Kiosk"
  key_links:
    - from: "run.py"
      to: "output/leads.xlsx"
      via: "full sample run"
      pattern: "42"
---

<objective>
Final Definition-of-Done verification: prove the full sample run is reproducible, the suite
is green, and all Phase 7 docs/hardening are in place. Then a human sign-off on AC9/AC10/§6.

Purpose: this is the gate that closes the Definition of Done (CLAUDE.md §7). It verifies the
outputs of Plans 01-03 and 05 together rather than adding anything new.
Output: a verified, reproducible run + passing tests + human approval.

NOTE: No new features (CLAUDE.md §6). Verification only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@CLAUDE.md
@README.md
@docs/sample_run_rationale.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full suite and the full sample end-to-end</name>
  <read_first>CLAUDE.md (§7 Definition of Done)</read_first>
  <files></files>
  <action>
1. Run the test suite: `python -m pytest -q`. All 202 tests MUST pass. If any fail, STOP and
   report — do not modify production code beyond what the failure strictly requires (scope §6).
2. Reproduce the full sample run with NO keys (fresh user). Bypass the cache for a true cold run:
   `python run.py data/sample_input.xlsx -o output/leads.xlsx --no-cache`
   Confirm it processes 42 rows and writes output/leads.xlsx without error or crash.
3. Confirm the Phase 7 artifacts exist and are coherent: README.md (Plan 01),
   docs/sample_run_rationale.md (Plan 02), the privacy audit (Plan 03 — `git ls-files` shows no
   .env/output/cache tracked), and the planning-state cleanup (Plan 05 — REQUIREMENTS.md has no
   "Pending"/"Complete"-mislabelled Phase 7 rows once this phase ships; ZK rows read Done).
Do NOT commit output/leads.xlsx (gitignored).
  </action>
  <verify>
    <automated>python -m pytest -q 2>&1 | tail -1 | grep -qE '202 passed|passed'; test -f output/leads.xlsx; test -f README.md; test -f docs/sample_run_rationale.md; ! git ls-files | grep -qE '^(\.env$|output/|cache/)'</automated>
  </verify>
  <done>202 tests pass; full 42-row run reproduced with --no-cache (no keys); README, rationale doc,
clean git tracking, and updated planning state all confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
README.md (setup in <5 min, no keys needed), docs/sample_run_rationale.md (AC10 rationale over the
full 42-row sample with edge-case + firm-size justification), verified privacy hardening
(.env/output/cache gitignored and untracked), and corrected planning state. Full suite green (202)
and full sample run reproduced.
  </what-built>
  <how-to-verify>
1. Open README.md. Confirm clone -> scored output file in under 5 minutes: install command, the one
   run command, all CLI flags, and the "runs without keys" note are all there.
2. Open docs/sample_run_rationale.md. Confirm the distributions and the rationale for the edge cases
   (empty-URL Kiosk -> Bedarf 5; broken-URL Nähatelier -> Bedarf 5) and the big-vs-small firm
   contrast (AG/Treuhand Z4-Z5 vs Coiffeur Z1) read as sensible.
3. Optionally run it: `python run.py data/sample_input.xlsx -o output/leads.xlsx` and open
   output/leads.xlsx — top rows should be high-Bedarf + high-Zahlungskraeftigkeit leads.
4. Confirm `git status` shows no .env or output/ staged.
This closes the Definition of Done (CLAUDE.md §7).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 7 (and the Definition of Done) complete, or describe what to fix.</resume-signal>
</task>

</tasks>

<verification>
- `python -m pytest` -> 202 passed.
- `python run.py data/sample_input.xlsx -o output/leads.xlsx --no-cache` -> 42 rows, no crash.
- No secrets/data tracked by git.
</verification>

<success_criteria>
Definition of Done met: reproducible no-key full run, green suite, README + rationale + privacy
hardening + corrected planning state present and human-approved (SETUP-01/02/03; AC9/AC10; §6/§7).
</success_criteria>

<output>
After completion, create `.planning/phases/07-hardening-readme-full-sample-run/07-04-SUMMARY.md`
</output>
