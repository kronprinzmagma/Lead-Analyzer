---
phase: 03-real-website-bedarf-score
plan: 04
type: tdd
wave: 2
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - lead_analyzer/pipeline.py
  - lead_analyzer/analyzers/existence.py
  - tests/test_pipeline_bedarf.py
  - tests/test_pipeline_dim1.py
autonomous: true
requirements: [BED-02, BED-04, BED-05, BED-06, BED-07, BED-08, NACH-01]
must_haves:
  truths:
    - "analyze_row parses HTML once and runs all six dimension verdicts (dim1 + dim2/4/5/6 + dim3 placeholder)"
    - "Bedarf comes from scoring.bedarf(6 verdicts); dim1 dead still overrides to 5"
    - "RowResult.reason = reasons.build(verdicts) → output Begründung column shows the driving dims"
    - "A modern crafted fixture scores 1; a broken/empty fixture scores 5 (direction, BED-08)"
    - "A reachable-but-no-body row (403/406/429, empty body) yields Bedarf != 5 — dims 4/5/6 return neutral, only dim1 gap (+ maybe dim2) score → typically Bedarf 2"
    - "All previously-green tests still pass; reason-TEXT assertions in test_pipeline_dim1.py reconciled to the reasons.build form"
  artifacts:
    - path: "lead_analyzer/pipeline.py"
      provides: "Full 6-dim analyze_row wiring with parse-once"
      contains: "scoring.bedarf"
    - path: "tests/test_pipeline_bedarf.py"
      provides: "Offline integration: 6-dim wiring + modern→1 / broken→5 + 403-no-body!=5 + reason threading"
  key_links:
    - from: "lead_analyzer/pipeline.py analyze_row"
      to: "BeautifulSoup parsed once, passed to seo/ai_readiness/content"
      via: "soup = BeautifulSoup(fr.html, 'html.parser') if fr.html else None"
      pattern: "BeautifulSoup"
    - from: "lead_analyzer/pipeline.py analyze_row"
      to: "scoring.bedarf(verdicts) + reasons.build(verdicts)"
      via: "aggregation + traceability"
      pattern: "reasons\\.build"
---

<objective>
Wire the full six-dimension Website-Bedarf score into the pipeline. Replace the provisional `bedarf_from_dim1` path in `analyze_row` with: parse HTML once, run dim1 (existence) + dim2 (technical) + dim4 (seo) + dim5 (ai_readiness) + dim6 (content) + the dim3 `ok` placeholder, aggregate via `scoring.bedarf`, and set `RowResult.reason = reasons.build(verdicts)` (threaded into the existing Begründung column). Refactor `existence.analyze` to accept an optional pre-parsed soup (fetch-once-parse-many). Keep `zahl` on its Phase-1 placeholder (Phase 4 owns it). All Phase-1/2 behavior preserved; all existing tests stay green (with reconciled reason-text + 403 expectations).

Purpose: This is the integration that makes BED-02/04/05/06/07/08 + NACH-01 observable end-to-end (the MVP "runnable" deliverable of the phase).
Output: updated pipeline.py + existence.py, plus tests/test_pipeline_bedarf.py, plus reconciled tests/test_pipeline_dim1.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-website-bedarf-score/03-RESEARCH.md
@docs/scoring_website_bedarf.md
@.planning/phases/03-real-website-bedarf-score/03-01-SUMMARY.md
@.planning/phases/03-real-website-bedarf-score/03-02-SUMMARY.md
@.planning/phases/03-real-website-bedarf-score/03-03-SUMMARY.md

<read_first>
- lead_analyzer/pipeline.py  (current analyze_row — MODIFY; keep the AC4 try/except per-row boundary + empty-URL no-net short-circuit + _zahl_placeholder)
- lead_analyzer/analyzers/existence.py  (REFACTOR: add optional soup param, backward compatible)
- lead_analyzer/scoring.py  (use bedarf + DIM3_PLACEHOLDER from 03-03; bedarf_from_dim1 may now be removed AFTER updating its callers/tests — see action)
- lead_analyzer/reasons.py  (build from 03-03)
- lead_analyzer/analyzers/{technical,seo,ai_readiness,content}.py  (from 03-01/03-02)
- tests/test_pipeline_dim1.py  (existing; analyze_row-level cases MUST be reconciled to the 6-dim path — see Task 3 note; the bedarf_from_dim1 unit tests stay as-is)
- tests/conftest.py  (make_fetch_result)
</read_first>

<interfaces>
Analyzer call signatures (from 03-01/03-02/03-03):
```python
technical.analyze(fr)                # Dim 2 — measured even when fr.html is None
seo.analyze(fr, soup)                # Dim 4 — NEUTRAL ("ok"/source "n/a") when soup is None
ai_readiness.analyze(soup)          # Dim 5 — NEUTRAL ("ok"/source "n/a") when soup is None
content.analyze(fr, soup)           # Dim 6 — NEUTRAL ("ok"/source "n/a") when soup is None
existence.analyze(fr, soup=None)    # Dim 1 (refactored this plan; soup optional, backward compatible)
scoring.DIM3_PLACEHOLDER            # DimensionVerdict(3,"ok",...) constant
scoring.bedarf(verdicts) -> int     # dead→5 else G/S bands
reasons.build(verdicts) -> str      # compact Begründung
```
RowResult(index, bedarf, zahl, reason="", verdicts=[]).

No-body invariant (from 03-01/03-02): for a reachable-but-no-body row (status 403/406/429 or empty body, NOT dead), dim1=gap, dim2=measured (real ok/severe), dims 4/5/6=neutral ("ok", 0 gap-points). So G/S counts stay low and `scoring.bedarf` returns a low value (typically 2). The 403/blocked != Bedarf 5 invariant therefore holds BY CONSTRUCTION at the analyzer layer.
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Refactor existence.analyze to accept optional pre-parsed soup</name>
  <files>lead_analyzer/analyzers/existence.py</files>
  <action>
Change signature to `def analyze(fr, soup=None) -> DimensionVerdict`. Where it currently does `soup = BeautifulSoup(fr.html, "html.parser")`, use the passed soup if given, else build it (backward compatible — existing test_existence.py calls `analyze(fr)` and must still pass). Guard the `fr.html` truthiness check as today. No behavior change otherwise. This honors fetch-once-parse-many so the pipeline can build one soup for all HTML dims. KEEP the existing 403/406/429 → gap "blockiert – nicht bewertbar" and the "Antwort ohne lesbaren Body" → gap branches exactly as they are (these are the non-dead no-body paths the invariant depends on).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_existence.py -q</automated>
  </verify>
  <done>existence.analyze(fr) and analyze(fr, soup) both work; test_existence.py green; same verdicts as before.</done>
</task>

<task type="tdd">
  <name>Task 2 (RED): Write tests/test_pipeline_bedarf.py</name>
  <files>tests/test_pipeline_bedarf.py</files>
  <behavior>
    Import `from lead_analyzer.pipeline import analyze_row`, `from lead_analyzer import fetch, scoring, reasons`, `from lead_analyzer.config import Config`, `from lead_analyzer.models import RowRecord`, `from conftest import make_fetch_result`. Pattern: `monkeypatch.setattr(fetch, "fetch", lambda c, cfg: make_fetch_result(...))` then call analyze_row. _URL_COL="Website".
    Cases:
    - SIX verdicts wired: after a normal fetch, `res.verdicts` has length 6 and dims == {1,2,3,4,5,6}. (Dim 3 is the placeholder, level "ok".)
    - modern → 1: craft html that passes all dims (https own domain via final_url="https://firma.ch/"; lang+title+meta-desc+canonical+1 H1; JSON-LD business type + 3 OG; form with email input + tel + mailto + Impressum link + current-year copyright). ssl_ok=True. Assert res.bedarf == 1.
    - broken/dead → 5: make_fetch_result(ok=False, status=None, html=None, error="nicht erreichbar") → res.bedarf == 5 (dim1 dead override survives the new path).
    - empty URL → 5 no network: analyze_row(RowRecord(0,{"Website":None}),...) with fetch spy that raises if called → bedarf 5, reason "keine Website", fetch NOT called.
    - **403-no-body != 5 (REGRESSION GUARD for the reviewed blocker):** make_fetch_result(ok=False, status=403, html=None). Assert (a) `res.bedarf != 5` (the hard invariant), and (b) `res.bedarf == 2` (the documented value: dim1 gap + dims 4/5/6 neutral + dim2 measured/ok on an https example → single gap-point → band 2). If dim2 also flags on the chosen fixture, the doc value is 3 — pick a clean-https fixture (final_url="https://waf.ch/", ssl_ok=True) so dim2 is ok and the expected value is exactly 2. Also assert "blockiert" appears in res.reason. (Mirror the value chosen here in the test_pipeline_dim1.py reconciliation in Task 3 — they MUST agree.)
    - http-only bumps Bedarf: a site identical to the modern fixture but final_url="http://firma.ch/" (Dim2 severe) → res.bedarf > 1 (monotonic; severe S>=1 → at least 3).
    - reason threaded: res.reason == reasons.build(res.verdicts) (compare exactly) OR contains "Bedarf" and a Dim label for the http-only case.
    - zahl stays placeholder: res.zahl == scoring.placeholder_result(rec).zahl.
    - per-row boundary intact: monkeypatch one analyzer (e.g. seo.analyze) to raise → analyze_row does NOT raise, returns degraded RowResult bedarf 5 reason startswith "Fehler:".
  </behavior>
  <action>Write tests/test_pipeline_bedarf.py (offline; autouse network block active). Do NOT modify pipeline yet. Confirm RED (length 6 / modern→1 / 403-no-body fail because pipeline still uses dim1 only).</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pipeline_bedarf.py -q 2>&1 | grep -qE "failed|error|AssertionError"</automated>
  </verify>
  <done>tests/test_pipeline_bedarf.py exists with all cases incl. the explicit 403-no-body `!= 5` AND `== 2` assertions; pytest RED.</done>
</task>

<task type="tdd">
  <name>Task 3 (GREEN): Rewire analyze_row for the full 6-dimension score + reconcile test_pipeline_dim1.py</name>
  <files>lead_analyzer/pipeline.py, tests/test_pipeline_dim1.py</files>
  <action>
Update imports: `from .analyzers import existence, technical, seo, ai_readiness, content`, `from . import reasons`, and `from bs4 import BeautifulSoup`. Keep `from . import fetch, scoring, table_io` and `_zahl_placeholder`.

Rewrite the success path of `analyze_row` (KEEP the outer try/except AC4 boundary and the empty-URL no-net short-circuit exactly as today):
```python
fr = fetch.fetch(candidates, config)           # wirft nie
soup = BeautifulSoup(fr.html, "html.parser") if fr.html else None  # PARSE ONCE
verdicts = [
    existence.analyze(fr, soup),               # Dim 1 (kann dead setzen)
    technical.analyze(fr),                      # Dim 2 (auch ohne Body messbar)
    scoring.DIM3_PLACEHOLDER,                   # Dim 3 (fix ok, Phase 6)
    seo.analyze(fr, soup),                      # Dim 4 (neutral wenn soup None)
    ai_readiness.analyze(soup),                 # Dim 5 (neutral wenn soup None)
    content.analyze(fr, soup),                  # Dim 6 (neutral wenn soup None)
]
bedarf = scoring.bedarf(verdicts)              # dead -> 5, sonst G/S-Bänder
return RowResult(
    record.index, bedarf=bedarf, zahl=_zahl_placeholder(record),
    reason=reasons.build(verdicts), verdicts=verdicts,
)
```
Notes:
- When `fr.html is None` (dead/unreachable), dim1 sets dead=True → scoring.bedarf returns 5 regardless of the other analyzers.
- When `fr.html is None` but the row is NOT dead (403/406/429, empty body), dim1=gap and dims 4/5/6 return NEUTRAL ("ok", 0 gap-points) while dim2 is measured → scoring.bedarf yields a LOW value (2 for a clean-https WAF row). The 403 != 5 invariant holds by construction — no special-casing needed in the pipeline.
- The empty-URL branch stays as-is (returns bedarf 5, reason "keine Website", no fetch).
- Keep the per-row try/except returning the degraded RowResult (bedarf 5, reason "Fehler: ...").
- Update the module/function docstring to describe the 6-dim flow.

### Reconcile tests/test_pipeline_dim1.py (MANDATORY — switching reason to reasons.build changes reason TEXT and 6-dim numbers)
The bedarf_from_dim1 UNIT tests (`test_bedarf_from_dim1_*`) call scoring.bedarf_from_dim1 directly — LEAVE THEM UNTOUCHED. Keep `scoring.bedarf_from_dim1` defined (now unused by the pipeline); add a one-line comment "# deprecated: superseded by bedarf(); kept for Phase-2 unit tests".

The analyze_row-level cases now exercise the NEW 6-dim path. Reconcile each:
1. `test_dead_unreachable_is_bedarf_5`: currently asserts `res.reason.startswith("nicht erreichbar")`. With `reason = reasons.build(verdicts)` the text becomes the reasons.build form (e.g. "Dim1 …(nicht erreichbar …) → Bedarf 5"), so `.startswith("nicht erreichbar")` WILL FAIL. UPDATE this assertion to match reasons.build's actual output — assert `"nicht erreichbar" in res.reason` (substring, not startswith) and keep `res.bedarf == 5` and `res.verdicts[0].dim == 1`. (Confirm against the reasons.build format from 03-03-SUMMARY; if its prefix differs, match the real string.)
2. `test_block_403_is_neutral_not_5`: currently asserts `res.bedarf != 5`, `res.bedarf == 3`, and `"blockiert" in res.reason`. With the 6-dim neutral policy a clean-https 403 row now scores **2**, not 3. UPDATE the equality to `res.bedarf == 2` (the documented value), KEEP the explicit `res.bedarf != 5` assertion (the load-bearing invariant), and confirm `"blockiert"` still appears in res.reason (it comes from dim1's gap reason via reasons.build — verify the substring survives the build join; if reasons.build truncates, assert against the term it actually emits for dim1). The expected value MUST equal the one asserted in test_pipeline_bedarf.py Task 2 (use the same clean-https fixture so both say 2).
3. `test_parked_is_bedarf_5`: parked html → dim1 dead=True → still 5. Verify; no change expected.
4. `test_social_only_high_but_not_5` and `test_reachable_thin_is_3`: these assert provisional Phase-2 single-dim numbers (4 and 3). The 6-dim score WILL differ (thin html with no structured markup/contact will add dim4/5/6 gaps/severes). RUN them; for each that fails, UPDATE the asserted number to the correct 6-dim value AND record the before→after in the SUMMARY as reconciled provisional numbers. Do NOT weaken any `!= 5` invariant and do NOT change production behavior to fake a pass.

Run the FULL suite and fix any provisional-number/reason-text drift in test_pipeline_dim1.py ONLY (never change behavior to fake a pass).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pipeline_bedarf.py tests/test_pipeline_dim1.py -q</automated>
  </verify>
  <done>test_pipeline_bedarf.py GREEN (6 verdicts, modern→1, broken→5, 403-no-body !=5 and ==2, http-only>1, reason threaded, zahl placeholder, boundary intact). test_pipeline_dim1.py green: bedarf_from_dim1 unit tests untouched; reason-text reconciled to substring; 403 row reconciled to ==2 with `!= 5` kept; other provisional numbers reconciled; dead→5 intact.</done>
</task>

<task type="tdd">
  <name>Task 4: Full-suite green gate + offline sample direction check</name>
  <files>tests/test_pipeline_bedarf.py</files>
  <action>
Add (if not already present in Task 2) one direction-gradient integration test in test_pipeline_bedarf.py asserting non-decreasing Bedarf as a crafted fixture worsens from modern (all dims good) → one severe (http-only) → unreachable (dead): bedarf sequence is non-decreasing and ends at 5. Then run the entire suite to confirm no regression anywhere (existing tests + all Phase-3 suites). No network may be touched (conftest autouse block must hold — if any test trips it, fix the test to mock fetch).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <done>Entire suite green; direction gradient test passes; the 403-no-body row asserts bedarf != 5 (== 2) by construction; zero real network calls (autouse block never raises).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party site → pipeline parse | Untrusted HTML parsed once offline, passed to pure analyzers; never executed |
| per-row failure → run | One bad row isolated by AC4 try/except boundary; run continues |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | DoS | analyzer raises mid-row | mitigate | Existing per-row try/except in analyze_row → degraded RowResult(bedarf 5), run continues (tested) |
| T-03-10 | DoS | single BeautifulSoup parse of large body | accept | 2 MB body cap upstream (fetch.py); parse once not per-analyzer |
| T-03-11 | Tampering | malformed HTML/JSON-LD reaching pipeline | mitigate | Analyzers guard soup=None (neutral) and wrap json.loads (03-01/02); html.parser tolerant |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` — entire suite green (existing + all Phase-3 plans).
- analyze_row builds exactly ONE BeautifulSoup per row and passes it to dims 1/4/5/6 (grep `BeautifulSoup` appears once in pipeline.py).
- res.verdicts length 6, dims {1..6}; res.reason == reasons.build(res.verdicts).
- dead/empty/broken rows → bedarf 5; modern fixture → 1; 403-no-body row → bedarf 2 (!= 5); no real network.
</verification>

<success_criteria>
- BED-02/04/05/06 observable end-to-end through analyze_row (six real/heuristic verdicts).
- BED-07: scoring.bedarf drives the score; dim1 dead → 5 override intact; 403/blocked != Bedarf 5 invariant holds by construction (dims 4/5/6 neutral on no-body).
- BED-08: direction monotonic in integration (modern→1, worsening non-decreasing, broken→5).
- NACH-01: Begründung column populated via reasons.build (six verdicts retained on RowResult).
- Parse-once honored; per-row boundary intact; zahl still placeholder; full suite green with reconciled reason-text + 403 expectations.

## SUMMARY note to record (03-04-SUMMARY.md)
- reason-text reconciliation: test_pipeline_dim1.py `test_dead_unreachable_is_bedarf_5` switched from `.startswith("nicht erreichbar")` to substring (`"nicht erreichbar" in reason`) because RowResult.reason now comes from reasons.build (the new compact "Dim… → Bedarf N" form). `"blockiert"` substring still holds for the 403 row.
- 403-no-body value: documented and asserted as Bedarf **2** (was provisional 3) — clean-https WAF row scores dim1 gap + dims 4/5/6 neutral + dim2 ok → single gap-point → band 2. The explicit `!= 5` invariant is kept. Same value asserted in both test_pipeline_bedarf.py and test_pipeline_dim1.py.
- any other reconciled provisional Phase-2 numbers (social-only, thin) recorded before→after.
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-website-bedarf-score/03-04-SUMMARY.md`.
</output>
</content>
</invoke>
