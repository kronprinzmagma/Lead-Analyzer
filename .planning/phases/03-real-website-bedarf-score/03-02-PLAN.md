---
phase: 03-real-website-bedarf-score
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lead_analyzer/analyzers/ai_readiness.py
  - lead_analyzer/analyzers/content.py
  - tests/test_ai_readiness.py
  - tests/test_content.py
autonomous: true
requirements: [BED-05, BED-06]
must_haves:
  truths:
    - "Dim 5 returns ok for JSON-LD business type + ≥3 OG tags, gap for OG/microdata-only, severe for nothing structured"
    - "Dim 5 never crashes on malformed JSON-LD (try/except), treats absence as a signal"
    - "Dim 6 detects contact path, tel/mailto, Impressum, and copyright/generator freshness via datetime.now().year (not hardcoded)"
  artifacts:
    - path: "lead_analyzer/analyzers/ai_readiness.py"
      provides: "Dim 5 KI-/Answer-Engine-Bereitschaft analyzer"
      contains: "def analyze"
    - path: "lead_analyzer/analyzers/content.py"
      provides: "Dim 6 Inhalt/Aktualität/Conversion analyzer"
      contains: "def analyze"
    - path: "tests/test_ai_readiness.py"
      provides: "BED-05 offline unit tests incl. malformed JSON-LD"
    - path: "tests/test_content.py"
      provides: "BED-06 offline unit tests incl. freshness bands"
  key_links:
    - from: "lead_analyzer/analyzers/ai_readiness.py"
      to: "json.loads in try/except"
      via: "defensive JSON-LD parse"
      pattern: "json\\.loads"
    - from: "lead_analyzer/analyzers/content.py"
      to: "datetime.now().year"
      via: "freshness vs current year"
      pattern: "datetime\\.now\\(\\)\\.year"
---

<objective>
Build the two heuristic Website-Bedarf dimensions: Dim 5 (KI-/Answer-Engine-Bereitschaft, BED-05) and Dim 6 (Inhalt, Aktualität & Conversion, BED-06). Both are pure analyzers reading the pre-parsed `soup` (Dim 6 also reads `fr` for the raw html copyright scan + `Last-Modified` corroboration). No network, no new dependency.

Purpose: AC11 requires dims 5/6 at least heuristically measured. They feed the aggregation (03-03) and are wired in 03-04.
Output: `analyzers/ai_readiness.py`, `analyzers/content.py`, and their offline tests. Tool stays runnable (not yet wired until 03-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-real-website-bedarf-score/03-RESEARCH.md
@docs/scoring_website_bedarf.md

<read_first>
- lead_analyzer/analyzers/existence.py  (STYLE: pure analyze, German docstring, [CITED:] markers)
- lead_analyzer/models.py  (DimensionVerdict)
- tests/conftest.py  (make_fetch_result — override html, headers)
- tests/test_existence.py  (test style)
</read_first>

<interfaces>
```python
@dataclass
class DimensionVerdict:
    dim: int; level: str; reason: str; source: str = "html"; dead: bool = False
```
make_fetch_result(**overrides) — defaults healthy; override html=..., headers=....
Tests import: `from conftest import make_fetch_result`, `from bs4 import BeautifulSoup`.
</interfaces>

## Sub-signal → dimension combination rule (Pattern 2, for Dim 6; pin EXACTLY)
Classes severe / gap / minor. Fold: any severe → "severe"; else ≥1 gap → "gap"; else ≥2 minor → "gap"; else "ok". Reason = compact German join of firing non-ok notes.

(Dim 5 uses its own explicit 3-way condition table below, NOT Pattern 2.)
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write tests/test_ai_readiness.py for Dim 5 (BED-05)</name>
  <files>tests/test_ai_readiness.py</files>
  <behavior>
    Import `from lead_analyzer.analyzers import ai_readiness`, `from bs4 import BeautifulSoup`.
    Signature is `ai_readiness.analyze(soup)` (soup only — no fr needed). Build soup from html strings.
    Cases (assert `.level`, `.dim == 5`):
    - ok: JSON-LD with business @type + ≥3 OG tags →
      html with `<script type="application/ld+json">{"@type":"LocalBusiness","name":"X"}</script>` and three `<meta property="og:title">`/`og:type`/`og:url` → "ok".
    - JSON-LD @type as a list: `{"@type":["Organization","LocalBusiness"]}` + ≥3 OG → "ok".
    - gap (OG only): some og: tags, no JSON-LD → "gap", reason mentions "kein JSON-LD".
    - gap (microdata only): `<div itemscope itemtype="...">` but no JSON-LD, no OG → "gap".
    - severe (nothing): plain html, no JSON-LD/OG/microdata → "severe", reason mentions "kein strukturiertes Markup".
    - malformed JSON-LD does NOT crash: `<script type="application/ld+json">{not valid json,,}</script>` with no OG → treated as no JSON-LD → "severe" (or gap if OG present); the call must not raise.
  </behavior>
  <action>Write tests/test_ai_readiness.py in existence-test style. Include the malformed-JSON-LD no-crash case explicitly. Do NOT implement. Confirm RED.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_ai_readiness.py -q 2>&1 | grep -qE "error|failed|ModuleNotFound|ImportError"</automated>
  </verify>
  <done>tests/test_ai_readiness.py exists; pytest RED.</done>
</task>

<task type="tdd">
  <name>Task 2 (GREEN): Implement lead_analyzer/analyzers/ai_readiness.py (Dim 5, BED-05)</name>
  <files>lead_analyzer/analyzers/ai_readiness.py</files>
  <action>
Pure Dim-5 analyzer. Imports: `from __future__ import annotations`, `import json`, `import re`, `from ..models import DimensionVerdict`. Signature `analyze(soup) -> DimensionVerdict`. Guard `soup is None` → DimensionVerdict(5, "severe", "kein HTML", "html").

Extract (verbatim recipe, [CITED: 03-RESEARCH.md Dim-5]):
```python
ld = soup.find_all("script", attrs={"type": "application/ld+json"})
types = set()
for s in ld:
    try:
        data = json.loads(s.string or s.get_text() or "")
    except (ValueError, TypeError):
        continue
    for obj in (data if isinstance(data, list) else [data]):
        if isinstance(obj, dict) and obj.get("@type"):
            t = obj["@type"]
            types.update(t if isinstance(t, list) else [t])
og = soup.find_all("meta", property=re.compile(r"^og:", re.I))
microdata = bool(soup.find(attrs={"itemscope": True}) or soup.find(attrs={"itemtype": True}))
```
A "business" @type = any non-empty `types` set (LocalBusiness/Organization/etc. all qualify; keep it inclusive — any structured @type counts as having JSON-LD).

Verdict table (explicit, NOT Pattern 2):
| Condition | Verdict |
|---|---|
| `types` non-empty AND `len(og) >= 3` | ok — f"JSON-LD {sorted(types)} + Open Graph vorhanden" |
| `types` non-empty (but <3 OG) OR (og or microdata) present, no full set | gap — "strukturiertes Markup unvollständig (kein JSON-LD oder zu wenig Open Graph)" |
| nothing (`not types and not og and not microdata`) | severe — "kein strukturiertes Markup" |

Implement as: if not types and not og and not microdata → severe; elif types and len(og)>=3 → ok; else → gap. Reason string German, mention what was found/missing. Source "html".
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_ai_readiness.py -q</automated>
  </verify>
  <done>All cases GREEN; malformed JSON-LD never raises; dim==5.</done>
</task>

<task type="tdd">
  <name>Task 3 (RED): Write tests/test_content.py for Dim 6 (BED-06)</name>
  <files>tests/test_content.py</files>
  <behavior>
    Import `from lead_analyzer.analyzers import content`, `from bs4 import BeautifulSoup`, `from conftest import make_fetch_result`, `from datetime import datetime`.
    Signature `content.analyze(fr, soup)`. Build soup from html; freshness tests compute the year relative to `datetime.now().year` (NEVER hardcode 2026) so the suite never rots.
    Cases (assert `.level`, `.dim == 6`):
    - clean ok: html with a `<form>` containing `<input type="email">`, an `<a href="tel:+41...">`, an `<a href="mailto:x@y.ch">`, a link text "Impressum", and copyright current year (`f"© {datetime.now().year} Firma"`) → "ok".
    - no contact path → gap: no form/email input AND no kontakt link → contributes gap.
    - no Impressum → gap: html missing impressum/datenschutz link → gap.
    - stale copyright gap: copyright year = now-2 → contributes gap.
    - stale copyright severe: copyright year = now-5 → "severe".
    - legacy generator → gap: `<meta name="generator" content="WordPress 4.9">` → gap.
    - missing tel AND mailto are minor: a page with form+impressum+fresh copyright but no tel and no mailto = 2 minor → folds to gap (per Pattern 2 ≥2 minor → gap). Assert "gap".
    - single minor only stays ok: form+impressum+fresh copyright+mailto present but no tel (1 minor) → "ok".
    - no copyright found is minor (not punished hard): page with form+impressum+tel+mailto, no copyright string → "ok" (single minor).
    - freshness uses datetime: build the copyright string from datetime.now().year ± n in the test itself.
  </behavior>
  <action>Write tests/test_content.py. Compute years from datetime.now().year. Do NOT implement content.py. Confirm RED.</action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_content.py -q 2>&1 | grep -qE "error|failed|ModuleNotFound|ImportError"</automated>
  </verify>
  <done>tests/test_content.py exists; pytest RED.</done>
</task>

<task type="tdd">
  <name>Task 4 (GREEN): Implement lead_analyzer/analyzers/content.py (Dim 6, BED-06)</name>
  <files>lead_analyzer/analyzers/content.py</files>
  <action>
Pure Dim-6 analyzer. Imports: `from __future__ import annotations`, `import re`, `from datetime import datetime`, `from ..models import DimensionVerdict`. Signature `analyze(fr, soup) -> DimensionVerdict`. Guard `soup is None` → DimensionVerdict(6, "severe", "kein HTML", "html"). `now = datetime.now().year`.

Sub-signal severity table (verbatim, [CITED: 03-RESEARCH.md Dim-6]):
| Sub-signal | Detect | Class |
|---|---|---|
| no contact path | no `<form>` containing `input[type=email]` or `<textarea>`, AND no `<a>` whose href or text matches `kontakt` (case-insensitive) | gap "kein Kontaktformular/-pfad" |
| no tel: | no `soup.select_one('a[href^="tel:"]')` | minor "kein tel:-Link" |
| no mailto: | no `soup.select_one('a[href^="mailto:"]')` | minor "kein mailto:-Link" |
| no Impressum/Datenschutz | no `<a>` whose text or href matches `impressum|rechtliches|datenschutz` (re.I) | gap "kein Impressum/Datenschutz" |
| stale copyright | scan raw html `re.search(r"(?:©|&copy;|copyright)[^\d]{0,12}(20\d{2})", fr.html or "", re.I)` → newest matched year. Bands vs `now`: `year >= now-1` ok · `now-3 <= year <= now-2` gap "Copyright {year}, veraltet" · `year <= now-4` severe "Copyright {year}, stark veraltet" · no match → minor "kein Copyright-Jahr gefunden" | ok/gap/severe/minor |
| legacy generator | `<meta name="generator">` content matches legacy: `WordPress [1-5]\.`, `Joomla! [123]\.`, `Drupal 7`, `FrontPage`, `Dreamweaver`, `Mobirise`, free `Jimdo` | gap "veralteter Generator {g}" |

Use ALL copyright matches and take `max(int(y) for y in re.findall(...))` for "newest year". For contact-link/impressum text matching, iterate `soup.find_all("a")` and check `(a.get("href") or "") + " " + a.get_text()` against the patterns. Keep regexes anchored/bounded (ReDoS-safe; the `{0,12}` bound is intentional).

Fold via Pattern 2 (any severe → severe; else ≥1 gap → gap; else ≥2 minor → gap; else ok). Reason = compact German join of firing non-ok notes; if ok → "Kontakt, Impressum, aktuell". `Last-Modified` header may be read as corroboration only (do not let it flip a band by itself — optional). Source "html".
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_content.py -q</automated>
  </verify>
  <done>All cases GREEN; year derived from datetime.now().year; Pattern-2 folding correct; dim==6.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| third-party site → analyzer | Untrusted HTML/JSON-LD captured in FetchResult; parsed offline, never executed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering/DoS | malformed JSON-LD in ai_readiness.py | mitigate | `json.loads` wrapped in `try/except (ValueError, TypeError)`; skip block, absence is a signal |
| T-03-05 | DoS | copyright/generator regex over attacker HTML | mitigate | Anchored, bounded `{0,12}` quantifier; no nested unbounded quantifiers |
| T-03-06 | DoS | bs4 parse huge HTML | accept | 2 MB body cap upstream (fetch.py); html.parser tolerant |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_ai_readiness.py tests/test_content.py -q` green.
- `.venv/bin/python -m pytest -q` full suite green; no network.
- ai_readiness.analyze has no `fr` param; content.analyze reads fr.html for the copyright scan; neither builds HTTP.
</verification>

<success_criteria>
- BED-05: Dim 5 heuristically measured (JSON-LD + OG + microdata), malformed-JSON-LD safe.
- BED-06: Dim 6 heuristically measured (contact/tel/mailto/Impressum/copyright/generator) with year from datetime.now().year.
- Pattern-2 folding pinned; full suite green.
</success_criteria>

<output>
After completion, create `.planning/phases/03-real-website-bedarf-score/03-02-SUMMARY.md`.
</output>
