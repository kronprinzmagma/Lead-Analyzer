---
phase: 06-optional-pagespeed-rate-limiting
plan: 02
type: tdd
wave: 1
depends_on: [01]
files_modified:
  - lead_analyzer/models.py
  - lead_analyzer/config.py
  - lead_analyzer/cli.py
autonomous: true
requirements: [PERF-02, BED-03]

must_haves:
  truths:
    - "A stdlib .env loader parses KEY=VALUE, ignores comments/blank lines, strips quotes, never overrides an existing env var, never raises on a missing file (AC9)"
    - "PsResult is a small JSON-serializable dataclass usable by the analyzer and client"
    - "cli.main() loads .env before building Config and supports --no-pagespeed (forces use_pagespeed=False)"
    - "load_dotenv is called exactly once, before Config construction"
  artifacts:
    - path: "lead_analyzer/config.py"
      provides: "load_dotenv() stdlib loader + use_pagespeed/pagespeed_concurrency/pagespeed_budget fields"
      contains: "def load_dotenv"
    - path: "lead_analyzer/models.py"
      provides: "PsResult dataclass"
      contains: "class PsResult"
    - path: "lead_analyzer/cli.py"
      provides: "--no-pagespeed flag + load_dotenv() call site"
      contains: "no-pagespeed"
  key_links:
    - from: "lead_analyzer/cli.py"
      to: "lead_analyzer/config.py:load_dotenv"
      via: "call in main() before Config(...)"
      pattern: "load_dotenv\\("
    - from: "lead_analyzer/cli.py"
      to: "Config.use_pagespeed"
      via: "use_pagespeed=not args.no_pagespeed"
      pattern: "use_pagespeed"
---

<objective>
Wave 1 slice A: the configuration + data-model groundwork that the analyzer (Plan 03) and the
PSI client (Plan 04) build on — the stdlib `.env` loader (AC9, zero new deps), the `PsResult`
dataclass, the `--no-pagespeed` CLI flag, and PSI tuning fields on Config.

Purpose: PSI defaults OFF without a key; `--no-pagespeed` always forces off; keys load from `.env`
without clobbering a real exported env var. This is the degradation-contract gate.
Output: load_dotenv() in config.py, PsResult in models.py, --no-pagespeed in cli.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-optional-pagespeed-rate-limiting/06-RESEARCH.md

<read_first>
- lead_analyzer/config.py — current Config dataclass (already has use_pagespeed: bool = True). You ADD load_dotenv() and the pagespeed tuning fields. Change use_pagespeed default policy via from_config in Plan 04, NOT here.
- lead_analyzer/models.py — dataclass conventions (asdict-friendly, JSON-native fields). PsResult goes here.
- lead_analyzer/cli.py — build_parser() + main(); add the flag and the load_dotenv() call.
- 06-RESEARCH.md "Tiny stdlib .env loader" and "PsResult dataclass" code examples — implement those verbatim in shape.
- tests/test_env_loader.py (from Plan 01) — the exact behavior your loader must satisfy.
</read_first>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: stdlib load_dotenv() + PsResult dataclass</name>
  <files>lead_analyzer/config.py, lead_analyzer/models.py</files>
  <behavior>
    load_dotenv(path=".env"):
    - Missing file → return None, raise nothing.
    - Each line: strip; skip blank, skip lines starting with "#", skip lines without "=".
    - key, _, val = line.partition("="); key.strip(); val.strip().strip('"').strip("'").
    - os.environ.setdefault(key, val) — NEVER override an existing var.
    - Wrap file read in try/except OSError → return (never raises).
    (Validated by tests/test_env_loader.py from Plan 01.)

    PsResult: dataclass(perf_score: float|None=None, lcp_ms: float|None=None,
      cls: float|None=None, tbt_ms: float|None=None, ok: bool=True). Plain fields so
      __dict__ / asdict round-trips through cache.put/get as JSON.
  </behavior>
  <action>
    In lead_analyzer/config.py: add `import os` and `from pathlib import Path`, then add the
    module-level `load_dotenv(path: str = ".env") -> None` exactly per 06-RESEARCH.md "Tiny stdlib
    .env loader" (setdefault, comment/blank/quote handling, OSError-safe). Also add three Config
    fields: `pagespeed_concurrency: int = 2`, `pagespeed_budget: int = 400` (keep existing
    use_pagespeed/timeout fields). German docstring + CITED marker referencing 06-RESEARCH.md.

    In lead_analyzer/models.py: add the `PsResult` dataclass per 06-RESEARCH.md "PsResult dataclass",
    placed near the other dataclasses, with a German docstring noting it is JSON-serializable for the
    PSI cache namespace and that `None` (not ok=False) signals failure.

    Do NOT touch cli.py here (Task 2). Do NOT wire pipeline (Plan 05).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_env_loader.py -x -q</automated>
  </verify>
  <done>test_env_loader.py green; PsResult importable from lead_analyzer.models; make_ps_result helper now resolves; Config has pagespeed_concurrency/pagespeed_budget.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: --no-pagespeed flag + load_dotenv() call site in cli.main()</name>
  <files>lead_analyzer/cli.py</files>
  <behavior>
    - build_parser adds: `--no-pagespeed` (action="store_true") with a German help string
      ("PageSpeed-Anreicherung von Dim 3 abschalten (erzwingt Heuristik)").
    - main(): FIRST line of the function body calls `load_dotenv()` (before Config is built) so
      Config/from_config can read os.environ["PAGESPEED_API_KEY"]. Single call site.
    - Config(...) gets `use_pagespeed=not args.no_pagespeed`.
    - --no-pagespeed → use_pagespeed False regardless of key presence (forces off).
  </behavior>
  <action>
    Edit lead_analyzer/cli.py: import load_dotenv from .config. Add the --no-pagespeed argument in
    build_parser() next to --no-cache. In main(), call `load_dotenv()` as the first statement, then
    pass `use_pagespeed=not args.no_pagespeed` into the Config(...) constructor. German comment on the
    call site explaining it must precede Config so the key is visible to from_config (Plan 04).

    Keep all existing flags/behavior intact. No pipeline changes here.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from lead_analyzer.cli import build_parser; ns=build_parser().parse_args(['in.xlsx','--no-pagespeed']); assert ns.no_pagespeed is True; print('OK')"</automated>
  </verify>
  <done>--no-pagespeed parses to True; load_dotenv imported and called once in main() before Config; use_pagespeed wired from the flag; existing CLI tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env file → os.environ | Untrusted KEY=VALUE file loaded into the process environment |
| CLI args → Config | User-controlled flags gate the optional PSI capability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering / Elevation | load_dotenv overriding a real exported secret | mitigate | os.environ.setdefault — a real exported var always wins over the file |
| T-06-04 | Denial of service | malformed .env crashing startup | mitigate | try/except OSError + per-line tolerant parse; never raises |
| T-06-05 | Information disclosure | PAGESPEED_API_KEY echoed to stdout/logs | accept | Key is never printed; only presence is checked in from_config (Plan 04) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_env_loader.py -q` green.
- `.venv/bin/python -m pytest -q` still ≥177 green (no regressions from the new fields/flag).
- PsResult round-trips: `PsResult(**PsResult(perf_score=0.5).__dict__)` equals the original.
</verification>

<success_criteria>
- load_dotenv() implemented per spec; all env-loader tests green.
- PsResult dataclass exists and is JSON-serializable via __dict__.
- --no-pagespeed flag present; load_dotenv called once before Config in main().
</success_criteria>

<output>
After completion, create `.planning/phases/06-optional-pagespeed-rate-limiting/06-02-SUMMARY.md`
</output>
