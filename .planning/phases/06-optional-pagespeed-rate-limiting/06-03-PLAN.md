---
phase: 06-optional-pagespeed-rate-limiting
plan: 03
type: tdd
wave: 1
depends_on: [01]
files_modified:
  - lead_analyzer/analyzers/performance.py
autonomous: true
requirements: [BED-03]

must_haves:
  truths:
    - "performance.analyze(fr, soup, None) returns a viewport-only verdict (present→ok, absent→gap) with a '(PageSpeed übersprungen/Fehler)' note — NEVER severe"
    - "With a real PsResult, the verdict refines via Lighthouse perf/LCP/CLS/TBT bands; the worst metric wins"
    - "A PSI failure (ps_result is None) can never lower the verdict below the viewport baseline (inversion guard)"
    - "Missing viewport with a good PSI result is at least a gap"
  artifacts:
    - path: "lead_analyzer/analyzers/performance.py"
      provides: "pure Dim-3 analyzer: viewport baseline + optional Lighthouse refinement"
      contains: "def analyze"
      min_lines: 40
  key_links:
    - from: "lead_analyzer/analyzers/performance.py"
      to: "lead_analyzer.models.DimensionVerdict"
      via: "returns DimensionVerdict(3, ...)"
      pattern: "DimensionVerdict\\(\\s*3"
    - from: "performance.analyze"
      to: "ps_result is None branch"
      via: "viewport-only fallback identical to not-attempted"
      pattern: "ps_result is None"
---

<objective>
Wave 1 slice B: the pure, offline Dimension-3 analyzer. It ALWAYS derives a baseline from the
viewport-meta tag in the already-parsed soup, and OPTIONALLY refines with a Lighthouse result.
The single most important property (Pitfall 1/8): a PSI error or absent result is treated
IDENTICALLY to "PSI never attempted" — viewport-only, never scored as a slow site.

Purpose: Implement BED-03 at the HTML level so Dim 3 is "really measured" even offline (AC11),
while guarding against the score-inversion that a naive PSI integration would cause (AC3/AC8).
Output: lead_analyzer/analyzers/performance.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-optional-pagespeed-rate-limiting/06-RESEARCH.md
@.planning/research/FEATURES.md

<read_first>
- lead_analyzer/analyzers/seo.py — analyzer signature convention `analyze(fr, soup)`, parse-once contract (NEVER build soup itself), DimensionVerdict aggregation style, German docstrings + CITED markers.
- lead_analyzer/analyzers/existence.py — style reference for soup-None handling and verdict construction.
- lead_analyzer/models.py — DimensionVerdict(dim, level, reason, source, dead) and PsResult (added in Plan 02).
- 06-RESEARCH.md Pattern 2 (PSI-error-is-not-slow), Pattern 3 (Lighthouse band mapping with exact thresholds), Pattern 4 — implement these verbatim in shape.
- tests/test_performance.py (Plan 01) — the exact behaviors this must satisfy.
</read_first>

<interfaces>
From lead_analyzer/models.py:
  DimensionVerdict(dim: int, level: str, reason: str, source: str = "html", dead: bool = False)
  PsResult(perf_score, lcp_ms, cls, tbt_ms, ok=True)  # any field may be None

Exact thresholds (06-RESEARCH.md Pattern 3 / FEATURES.md Dim 3):
  perf_score: >=0.90 ok / >=0.50 gap / else severe
  lcp_ms:     <=2500 ok / <=4000 gap / else severe
  cls:        <=0.10 ok / <=0.25 gap / else severe
  tbt_ms:     <=200  ok / <=600  gap / else severe
  worst band across present metrics wins; no usable metric → ok (do not penalize)
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: viewport detection + None-fallback branch (inversion guard)</name>
  <files>lead_analyzer/analyzers/performance.py</files>
  <behavior>
    _has_viewport_meta(soup) -> bool: True iff soup is not None AND a `<meta name="viewport" ...>`
    (name match case-insensitive) exists with a non-empty content attribute. soup=None → False.

    analyze(fr, soup, ps_result=None) -> DimensionVerdict, dim=3:
    - if ps_result is None:  (NOT attempted OR any failure — IDENTICAL branch)
        has_viewport → DimensionVerdict(3, "ok",  "viewport-meta vorhanden (PageSpeed übersprungen/Fehler)", "heuristic-fallback")
        else         → DimensionVerdict(3, "gap", "kein viewport-meta (PageSpeed übersprungen/Fehler)",     "heuristic-fallback")
      This branch NEVER returns "severe" (golden inversion-guard test).
  </behavior>
  <action>
    Create lead_analyzer/analyzers/performance.py with a module docstring (German) stating: pure/offline,
    parse-once (consumes the shared soup, never builds its own), and the inversion-guard contract
    (ps_result None == not attempted == viewport-only, never severe). Implement `_has_viewport_meta` and
    the `analyze` None-branch exactly as above. Reference 06-RESEARCH.md Pattern 2 with a CITED marker.
    Leave the PsResult-refinement branch as a follow-on in Task 2 (a temporary `return` of the viewport
    verdict is fine until Task 2, but the None-branch tests must pass now).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_performance.py::test_viewport_present_no_psi_is_ok tests/test_performance.py::test_no_viewport_no_psi_is_gap tests/test_performance.py::test_psi_error_not_scored_slow -x -q</automated>
  </verify>
  <done>The three None-branch / inversion-guard tests pass; dim==3; source=="heuristic-fallback"; never severe on ps_result=None.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Lighthouse band refinement (worst-metric-wins)</name>
  <files>lead_analyzer/analyzers/performance.py</files>
  <behavior>
    _band_from_lighthouse(r) -> str: collect a band per present metric using the exact thresholds in
    <interfaces>; return the worst (severe>gap>ok); empty (all None) → "ok".
    _worse(a, b) -> str: returns the worse of two bands.

    analyze with a real ps_result (ps_result is not None):
    - level = _band_from_lighthouse(ps_result)
    - if not has_viewport: level = _worse(level, "gap")  (missing viewport is at least a gap)
    - return DimensionVerdict(3, level, f"PageSpeed mobile perf={ps_result.perf_score:.2f}" + ("" if has_viewport else "; kein viewport-meta"), "pagespeed")
    - Guard the perf_score format against None (use "n/a" if perf_score is None).
  </behavior>
  <action>
    Extend performance.py with `_band_from_lighthouse` and `_worse` per 06-RESEARCH.md Pattern 3
    (exact thresholds, ordered max). Replace the temporary Task-1 return for the ps_result-not-None
    path with the refinement logic above. Keep the None-branch from Task 1 untouched and FIRST.
    German comments; CITED marker on the threshold table (FEATURES.md Dim 3).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_performance.py -x -q</automated>
  </verify>
  <done>All test_performance.py tests pass (≥8), incl. low-perf→severe(source=pagespeed), good-perf→ok, worst-metric-band, good-perf-no-viewport→at-least-gap. Full suite unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PSI result → score | An external/optional result must not be able to inflate Bedarf via a failure path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Tampering (score inversion) | analyze() None vs slow conflation | mitigate | `if ps_result is None` branch FIRST, returns viewport-only, structurally cannot return severe |
| T-06-07 | Denial of service (malformed metric) | perf_score None in f-string | mitigate | None-guard the format; absent metrics excluded from band set (no exception) |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_performance.py -q` fully green.
- Inversion guard explicit: analyze(fr_viewport, soup_viewport, None) → level "ok", not "severe".
- `.venv/bin/python -m pytest -q` still ≥177 + new green (analyzer is not yet wired — Plan 05).
</verification>

<success_criteria>
- performance.py implements viewport baseline + Lighthouse refinement with worst-metric-wins.
- ps_result=None path never yields severe (golden test passes).
- Thresholds match FEATURES.md / 06-RESEARCH.md exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/06-optional-pagespeed-rate-limiting/06-03-SUMMARY.md`
</output>
