---
phase: 06-optional-pagespeed-rate-limiting
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_env_loader.py
  - tests/test_performance.py
  - tests/test_pagespeed_client.py
  - tests/test_pipeline_bedarf.py
autonomous: true
requirements: [BED-03, PERF-02]

must_haves:
  truths:
    - "Failing test files exist for the .env loader, the performance analyzer, and the PSI client before any implementation"
    - "The PSI-error-not-slow golden test exists and currently fails (no analyzer yet)"
    - "conftest exposes make_ps_result() and a reusable fake-requests.get pattern for offline PSI tests"
    - "The full suite still collects without import errors (RED tests fail on missing modules, not on collection)"
  artifacts:
    - path: "tests/test_env_loader.py"
      provides: "RED tests for stdlib .env loader (comments/blank/quotes/no-override/missing-file)"
      min_lines: 30
    - path: "tests/test_performance.py"
      provides: "RED tests for Dim-3 analyzer incl. inversion guard"
      min_lines: 50
    - path: "tests/test_pagespeed_client.py"
      provides: "RED tests for PSI client (200/429/timeout/malformed/budget/semaphore/cache)"
      min_lines: 60
    - path: "tests/conftest.py"
      provides: "make_ps_result helper + PSI fake-get pattern"
      contains: "def make_ps_result"
  key_links:
    - from: "tests/conftest.py"
      to: "lead_analyzer.models.PsResult"
      via: "lazy import inside make_ps_result (tolerates RED phase)"
      pattern: "make_ps_result"
---

<objective>
Wave 0 of Phase 6 (Nyquist gate): create the failing test scaffolds that every later plan
verifies against. Per 06-VALIDATION.md and the RESEARCH "Wave 0 Gaps" list, no Phase-6
production code may be written until these tests exist and fail for the right reason.

Purpose: Lock the degradation contract (PSI error/None ≠ slow site) and the offline
byte-identical guarantee as executable tests BEFORE the analyzer/client exist.
Output: Three new test files, a `make_ps_result` conftest helper, and an extended
`test_pipeline_bedarf.py`, all RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-optional-pagespeed-rate-limiting/06-RESEARCH.md
@.planning/phases/06-optional-pagespeed-rate-limiting/06-VALIDATION.md

<read_first>
- tests/conftest.py — autouse network block + `make_fetch_result` + `FakeResponse`. You will ADD `make_ps_result` here. Note the network block is monkeypatch-based and per-test overridable.
- lead_analyzer/models.py — DimensionVerdict shape (dim, level, reason, source, dead). PsResult does NOT yet exist; tests import it lazily so collection survives.
- lead_analyzer/scoring.py — DIM3_PLACEHOLDER (level="ok") and the monotonic `bedarf()`.
- tests/test_scoring_bedarf.py, tests/test_pipeline_bedarf.py — existing patterns to mirror.
</read_first>

<interfaces>
Contracts the tests assert against (these are what Waves 1-3 must implement):

# lead_analyzer/config.py  (Wave 1)
def load_dotenv(path: str = ".env") -> None   # KEY=VALUE, setdefault, never raises

# lead_analyzer/models.py  (Wave 1)
@dataclass
class PsResult:
    perf_score: float | None = None   # 0..1
    lcp_ms: float | None = None
    cls: float | None = None
    tbt_ms: float | None = None
    ok: bool = True

# lead_analyzer/analyzers/performance.py  (Wave 1)
def analyze(fr, soup, ps_result=None) -> DimensionVerdict   # dim=3

# lead_analyzer/clients/pagespeed.py  (Wave 2)
class PageSpeedClient:
    @classmethod
    def from_config(cls, config) -> "PageSpeedClient | None"
    def is_available(self) -> bool
    def score(self, url: str) -> "PsResult | None"   # NEVER raises
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Task 1: conftest make_ps_result helper + .env loader RED tests</name>
  <files>tests/conftest.py, tests/test_env_loader.py</files>
  <behavior>
    make_ps_result(**overrides): mirrors make_fetch_result — lazy-imports lead_analyzer.models.PsResult,
    defaults perf_score=0.95, lcp_ms=2000, cls=0.05, tbt_ms=100, ok=True; each field overridable.
    Lazy import so collection survives while PsResult does not yet exist.

    test_env_loader.py (asserts config.load_dotenv):
    - Test: a .env in tmp_path with `KEY=value`, a `# comment` line, a blank line, and
      `QUOTED="val"` / `SINGLE='val'` → after load_dotenv(that path), os.environ has KEY=value,
      QUOTED=val, SINGLE=val (quotes stripped); comment/blank ignored.
    - Test: load_dotenv does NOT override an already-set os.environ var (setdefault semantics).
      Set os.environ["PRESET"]="real" first; .env has PRESET=fromfile; assert still "real".
    - Test: missing file path → load_dotenv returns None and raises nothing.
    - Use monkeypatch.delenv/setenv for isolation; never leak into real environment.
  </behavior>
  <action>
    Add `make_ps_result(**overrides)` to tests/conftest.py at module level (next to make_fetch_result),
    using a lazy `from lead_analyzer.models import PsResult` inside the function so collection does
    not break before Wave 1. German docstring + type hints, matching existing conftest style.

    Create tests/test_env_loader.py importing `from lead_analyzer.config import load_dotenv`.
    Write the four behaviors above. Use tmp_path for the .env file and monkeypatch for os.environ
    isolation. These tests MUST FAIL now (load_dotenv does not exist) — that is the RED state.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_env_loader.py -q 2>&1 | grep -qE "error|Error|failed|no attribute|cannot import" && echo "RED-OK"</automated>
  </verify>
  <done>make_ps_result present in conftest; test_env_loader.py has ≥4 tests; suite still collects (pytest --collect-only succeeds); env tests fail on missing load_dotenv.</done>
</task>

<task type="tdd">
  <name>Task 2: performance analyzer RED tests (incl. inversion guard)</name>
  <files>tests/test_performance.py</files>
  <behavior>
    Import `from lead_analyzer.analyzers import performance` and use make_fetch_result + make_ps_result.
    Build soup with `BeautifulSoup(html, "html.parser")` where html has/lacks `<meta name="viewport" content="width=device-width">`.

    - test_viewport_present_no_psi_is_ok: analyze(fr, soup_with_viewport, None) → level=="ok", source contains "heuristic-fallback", reason contains "übersprungen/Fehler".
    - test_no_viewport_no_psi_is_gap: analyze(fr, soup_without_viewport, None) → level=="gap".
    - test_psi_error_not_scored_slow (GOLDEN, inversion guard): analyze(fr, soup_with_viewport, ps_result=None) → level=="ok", NOT "severe". Explicit assert level != "severe".
    - test_psi_low_perf_severe: analyze(fr, soup_with_viewport, make_ps_result(perf_score=0.30)) → level=="severe", source=="pagespeed".
    - test_psi_good_perf_ok: analyze(fr, soup_with_viewport, make_ps_result(perf_score=0.95)) → level=="ok", source=="pagespeed".
    - test_worst_metric_band: make_ps_result(perf_score=0.95, lcp_ms=5000) (LCP severe) → level=="severe" (worst metric wins).
    - test_psi_good_but_no_viewport_at_least_gap: make_ps_result(perf_score=0.95) + soup_without_viewport → level in ("gap","severe"), not "ok".
    - All verdicts: dim==3.
  </behavior>
  <action>
    Create tests/test_performance.py with the eight behaviors above. Define two small helper soups
    inline (viewport-present and viewport-absent). Reuse make_fetch_result(html=...) for fr.
    German comments. Tests MUST FAIL now (performance module does not exist) — RED state.
    Do NOT implement the analyzer here.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_performance.py -q 2>&1 | grep -qE "error|Error|failed|cannot import|no attribute" && echo "RED-OK"</automated>
  </verify>
  <done>test_performance.py has ≥8 tests including test_psi_error_not_scored_slow; all fail on missing performance module; suite still collects.</done>
</task>

<task type="tdd">
  <name>Task 3: PSI client RED tests (mocked, injected sleep) + pipeline --no-pagespeed test</name>
  <files>tests/test_pagespeed_client.py, tests/test_pipeline_bedarf.py</files>
  <behavior>
    test_pagespeed_client.py — fully offline. Build clients via PageSpeedClient(...) directly with an
    injected no-op sleep (sleep=lambda *_: None) and a small Semaphore/budget, OR via from_config with
    monkeypatch.setenv("PAGESPEED_API_KEY","k") and a Config(use_pagespeed=True). Monkeypatch
    `requests.get` to a fake returning a FakeResponse-like object (status_code, json(), headers).
    A valid PSI body: {"lighthouseResult": {"categories": {"performance": {"score": 0.42}},
      "audits": {"largest-contentful-paint": {"numericValue": 3000},
                 "cumulative-layout-shift": {"numericValue": 0.05},
                 "total-blocking-time": {"numericValue": 150}}}}.

    - test_unavailable_without_key: from_config with no PAGESPEED_API_KEY → returns None (client) OR is_available() False.
    - test_200_parsed: score(url) → PsResult(perf_score≈0.42, lcp_ms==3000, cls==0.05, tbt_ms==150).
    - test_timeout_returns_none: fake requests.get raises requests.exceptions.Timeout → score()==None, no raise.
    - test_malformed_json_none: 200 but .json() raises ValueError → score()==None.
    - test_429_retry_after_capped: fake returns 429 with Retry-After header every time; injected sleep
      records calls; after capped retries (≤3) score()==None and sleep was called (backoff happened),
      NO real wait.
    - test_budget_exhausted_skips: budget=0 (or consumed) → score() returns None WITHOUT calling requests.get
      (assert the fake get was not invoked).
    - test_cache_hit_no_network: pre-put a PsResult dict at cache.key_for(["pagespeed-v1","mobile",url]);
      score(url) → returns it WITHOUT calling requests.get and WITHOUT consuming budget.
    - test_semaphore_caps: with Semaphore(2), a fake get that records max concurrent entries across
      threads never exceeds 2 (use threading + a counter guarded by a Lock in the fake).

    test_pipeline_bedarf.py — ADD:
    - test_no_pagespeed_flag: a Config(use_pagespeed=False) run path → PageSpeedClient.from_config returns None,
      analyze_row uses heuristic (ps_result None), Bedarf for a viewport-present reachable fixture is
      UNCHANGED vs the current placeholder result.
  </behavior>
  <action>
    Create tests/test_pagespeed_client.py with the eight client behaviors. Import
    `from lead_analyzer.clients.pagespeed import PageSpeedClient` and `from lead_analyzer.models import PsResult`.
    Use monkeypatch.setattr(requests, "get", fake) — the autouse network block is overridable per test (LIFO).
    Inject sleep so NO test waits. Use the tmp cache dir already provided by the _isolate_cache fixture for
    the cache-hit test (cache.put then score). German comments.

    Append test_no_pagespeed_flag to tests/test_pipeline_bedarf.py mirroring its existing run/fixture style.

    All new tests MUST FAIL now (no clients package / no from_config wiring). RED state. Do NOT implement.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pagespeed_client.py tests/test_pipeline_bedarf.py::test_no_pagespeed_flag -q 2>&1 | grep -qE "error|Error|failed|cannot import|no attribute" && echo "RED-OK"</automated>
  </verify>
  <done>test_pagespeed_client.py has ≥8 tests; test_no_pagespeed_flag added; all new tests fail on missing client/wiring; existing 177 tests still collect and pass (only NEW tests are RED).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → network | Tests must never hit real PSI; autouse conftest blocks it, per-test fakes override |
| .env file → process env | Untrusted file contents loaded into os.environ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information disclosure | real PAGESPEED_API_KEY in tests | mitigate | Tests use a dummy key "k" only; never read a real key; network blocked by autouse fixture |
| T-06-02 | Denial of service | real sleep in 429 backoff test | mitigate | Inject no-op sleep; assert backoff via call recording, never wall-clock wait |
</threat_model>

<verification>
- `.venv/bin/python -m pytest --collect-only -q` succeeds (no collection/import errors).
- New test files exist and FAIL for the right reason (missing modules), not on syntax/collection.
- Existing 177 tests still pass (RED is confined to new tests).
</verification>

<success_criteria>
- tests/test_env_loader.py, tests/test_performance.py, tests/test_pagespeed_client.py created and RED.
- make_ps_result in conftest; test_no_pagespeed_flag appended to test_pipeline_bedarf.py.
- The inversion-guard golden test (test_psi_error_not_scored_slow) is present and asserts level != "severe".
- Suite collects cleanly; only new tests fail.
</success_criteria>

<output>
After completion, create `.planning/phases/06-optional-pagespeed-rate-limiting/06-01-SUMMARY.md`
</output>
