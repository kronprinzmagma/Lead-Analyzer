---
phase: 06-optional-pagespeed-rate-limiting
plan: 04
type: tdd
wave: 2
depends_on: [01, 02]
files_modified:
  - lead_analyzer/clients/__init__.py
  - lead_analyzer/clients/pagespeed.py
autonomous: true
requirements: [PERF-02]

must_haves:
  truths:
    - "PageSpeedClient.from_config returns None unless use_pagespeed AND PAGESPEED_API_KEY is present (default OFF without key)"
    - "score(url) returns a parsed PsResult on a 200, and None on EVERY failure (timeout, non-200, 429 after capped retries, malformed JSON, budget-exhausted) — it NEVER raises"
    - "A cache hit returns the cached PsResult with no network call and no budget spend"
    - "A semaphore caps concurrent PSI calls below the worker count; backoff honors Retry-After with injected sleep; a per-run budget caps total calls"
  artifacts:
    - path: "lead_analyzer/clients/pagespeed.py"
      provides: "optional PSI client: is_available(), score()->PsResult|None, budget, semaphore, backoff"
      contains: "class PageSpeedClient"
      min_lines: 80
    - path: "lead_analyzer/clients/__init__.py"
      provides: "clients package marker"
  key_links:
    - from: "lead_analyzer/clients/pagespeed.py"
      to: "lead_analyzer.cache"
      via: "namespaced key cache.key_for(['pagespeed-v1','mobile',url])"
      pattern: "pagespeed-v1"
    - from: "PageSpeedClient.score"
      to: "requests.get"
      via: "GET PSI v5 under semaphore, returns None on RequestException"
      pattern: "requests.get"
---

<objective>
Wave 2: the optional, rate-limited PageSpeed Insights client. It is the ONLY component that touches
the network. It is gated by `is_available()`, capped by a semaphore + per-run budget, backs off on
429/5xx honoring Retry-After (injected sleep for testability), caches every result in a separate
namespace, and returns `None` on EVERY failure so a PSI problem can never crash or inflate a score.

Purpose: PERF-02/AC8 — external APIs use retry/backoff, respect Retry-After, have a per-run budget,
and an API error never aborts the run. AC7/AC8 — cached results skip the network on re-runs.
Output: lead_analyzer/clients/ package with pagespeed.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-optional-pagespeed-rate-limiting/06-RESEARCH.md
@.planning/research/PITFALLS.md

<read_first>
- lead_analyzer/cache.py — key_for(list[str]), get(key)->dict|None (never raises), put(key, dict) (atomic, thread-safe). Reuse with a namespaced key; do NOT add a new cache format.
- lead_analyzer/fetch.py — requests.Session/timeout discipline + RequestException handling style to mirror.
- lead_analyzer/config.py (after Plan 02) — use_pagespeed, pagespeed_concurrency (2), pagespeed_budget (400), timeout_connect, timeout_read.
- lead_analyzer/models.py (after Plan 02) — PsResult (round-trips via __dict__).
- 06-RESEARCH.md "Client availability + score skeleton" and Pattern 4 (endpoint + parse) — implement verbatim in shape.
- tests/test_pagespeed_client.py (Plan 01) — the exact behaviors this must satisfy.
</read_first>

<interfaces>
ENDPOINT = "https://pagespeedonline.googleapis.com/pagespeedonline/v5/runPagespeed"
params: {"url": url, "strategy": "mobile", "category": ["performance", "seo"], "key": key}

Parse (every access wrapped — any KeyError/TypeError/ValueError → return None):
  lr   = data["lighthouseResult"]
  perf = lr["categories"]["performance"]["score"]                 # 0..1
  lcp  = lr["audits"]["largest-contentful-paint"]["numericValue"] # ms
  cls  = lr["audits"]["cumulative-layout-shift"]["numericValue"]
  tbt  = lr["audits"]["total-blocking-time"]["numericValue"]      # ms

Cache key (separate namespace, Pitfall 3): cache.key_for(["pagespeed-v1", "mobile", url])
Constructor: PageSpeedClient(key, semaphore, budget, timeout, sleep=time.sleep)
from_config(config) -> PageSpeedClient | None
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: package + client skeleton — from_config, is_available, budget, cache hit</name>
  <files>lead_analyzer/clients/__init__.py, lead_analyzer/clients/pagespeed.py</files>
  <behavior>
    _Budget(n): try_consume() decrements and returns True while >0, else False; exhausted()->True when 0.
      Thread-safe via threading.Lock.
    PageSpeedClient.__init__(key, semaphore, budget, timeout, sleep=time.sleep): store all.
    from_config(config): if not getattr(config,"use_pagespeed",False) → None;
      key=os.environ.get("PAGESPEED_API_KEY"); if not key → None (default OFF without key);
      else return cls(key, threading.Semaphore(config.pagespeed_concurrency),
        _Budget(config.pagespeed_budget), (config.timeout_connect, max(config.timeout_read, 30.0))).
    is_available(): key is not None and not budget.exhausted().
    score(url):
      ck = cache.key_for(["pagespeed-v1","mobile",url]); cached=cache.get(ck);
      if cached is not None → return PsResult(**cached)  (NO network, NO budget spend);
      if not budget.try_consume() → return None;
      data = self._request(url); if data is None → None; res=_parse(data);
      if res is not None → cache.put(ck, res.__dict__); return res.
  </behavior>
  <action>
    Create lead_analyzer/clients/__init__.py (empty package marker with a one-line German docstring).
    Create lead_analyzer/clients/pagespeed.py with: module docstring (German) stating optional/network-only,
    returns None on every failure (never raises), namespaced cache, semaphore+budget+backoff (AC8/PERF-02).
    Implement `_Budget`, `PageSpeedClient.__init__`, `from_config`, `is_available`, and `score` with the
    cache-hit + budget gate per <interfaces>. Stub `_request` to return None and `_parse` for now (Task 2
    fills them) so the cache-hit / budget / availability tests pass. CITED markers referencing 06-RESEARCH.md.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pagespeed_client.py::test_unavailable_without_key tests/test_pagespeed_client.py::test_budget_exhausted_skips tests/test_pagespeed_client.py::test_cache_hit_no_network -x -q</automated>
  </verify>
  <done>Package importable; from_config returns None without key; budget-exhausted skips network; cache hit returns PsResult with no network/budget spend.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: _request (semaphore, backoff, Retry-After) + _parse — None on every failure</name>
  <files>lead_analyzer/clients/pagespeed.py</files>
  <behavior>
    _parse(data): defensively read perf/lcp/cls/tbt per <interfaces>; ANY KeyError/TypeError/ValueError
      → return None; else PsResult(perf_score, lcp_ms, cls, tbt_ms, ok=True).
    _parse_retry_after(header) -> float|None: int seconds if parseable, else None.
    _request(url): params per <interfaces>. Loop up to 3 attempts:
      - `with self._sem:` requests.get(ENDPOINT, params=params, timeout=self._timeout)
      - except requests.RequestException → return None
      - 200 → try r.json(); except ValueError → return None; else return the dict
      - status in (429,500,502,503,504) and attempt<2 → sleep(Retry-After+jitter OR (2**attempt)+jitter)
        via self._sleep (injected), then continue
      - any other status / exhausted retries → return None
    Concurrency: the semaphore must wrap the network call so no more than N are in flight.
  </behavior>
  <action>
    Implement `_request`, `_parse`, `_parse_retry_after` in pagespeed.py exactly per the 06-RESEARCH.md
    "Client availability + score skeleton" and Pattern 4. Use `import random, time` and `import requests`.
    The semaphore wraps ONLY the requests.get (acquire/release around the call). Jitter via random.uniform.
    Every failure path returns None — NEVER raise out of score()/_request()/_parse(). German comments;
    CITED markers on the endpoint, parse paths, and backoff. Do NOT change Task-1 code paths.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pagespeed_client.py -x -q</automated>
  </verify>
  <done>All test_pagespeed_client.py tests pass (≥8): 200→parsed PsResult, timeout→None, malformed→None, 429+Retry-After→capped backoff then None (injected sleep, no real wait), semaphore caps ≤2. score() never raises.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → PSI endpoint | Untrusted external response (status, headers, JSON) crosses here |
| PSI failure → run | A network/quota error must not propagate as a crash or a score change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Denial of service | unbounded retries / 429 storm | mitigate | capped retries (3), per-run budget, semaphore cap (2 < workers), honor Retry-After |
| T-06-09 | Tampering | malformed PSI JSON parsed into a bad score | mitigate | _parse wraps every access; any KeyError/TypeError/ValueError → None (degrade to viewport) |
| T-06-10 | Denial of service | one PSI failure crashing the per-row boundary | mitigate | score()/_request() return None on every requests.RequestException; never raise |
| T-06-11 | Information disclosure | API key in request URL leaking to logs | accept | key passed via requests params (standard PSI auth); never logged by the client |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_pagespeed_client.py -q` fully green.
- No real network: all tests monkeypatch requests.get; injected sleep means zero wall-clock backoff.
- `.venv/bin/python -m pytest -q` still ≥177 + new green (client not yet wired — Plan 05).
</verification>

<success_criteria>
- clients/pagespeed.py implements from_config/is_available/score with semaphore + budget + backoff + namespaced cache.
- score() returns PsResult on 200 and None on every failure; never raises.
- Cache hit spends no network and no budget; default OFF without key.
</success_criteria>

<output>
After completion, create `.planning/phases/06-optional-pagespeed-rate-limiting/06-04-SUMMARY.md`
</output>
