---
phase: 06-optional-pagespeed-rate-limiting
plan: 05
type: tdd
wave: 3
depends_on: [02, 03, 04]
files_modified:
  - lead_analyzer/pipeline.py
autonomous: true
requirements: [BED-03, PERF-02]

must_haves:
  truths:
    - "analyze_row builds Dim 3 from performance.analyze(fr, soup, ps_result), replacing scoring.DIM3_PLACEHOLDER"
    - "run() constructs ONE PageSpeedClient (shared semaphore + budget) per run via from_config; analyze_row receives it"
    - "ps_result is obtained only when the client is non-None AND is_available() AND the fetch was usable; otherwise None"
    - "The offline default run (no key / --no-pagespeed) produces output byte-identical to Phase 5 (viewport-present sites → Dim 3 ok = 0 gap-points = same Bedarf); all 177 tests stay green"
    - "A PSI error or quota never aborts the run and never raises out of analyze_row (per-row boundary intact)"
  artifacts:
    - path: "lead_analyzer/pipeline.py"
      provides: "Dim-3 wiring: performance verdict + one shared PSI client per run"
      contains: "performance.analyze"
  key_links:
    - from: "lead_analyzer/pipeline.py:run"
      to: "PageSpeedClient.from_config"
      via: "construct once, pass into analyze_row"
      pattern: "from_config"
    - from: "lead_analyzer/pipeline.py:analyze_row"
      to: "performance.analyze"
      via: "replaces scoring.DIM3_PLACEHOLDER in the verdict list"
      pattern: "performance.analyze"
---

<objective>
Wave 3: wire Dimension 3 into the pipeline. Replace the single `scoring.DIM3_PLACEHOLDER` line with
`performance.analyze(fr, soup, ps_result)`, construct ONE shared PageSpeedClient per run, and obtain
`ps_result` only when PSI is enabled+available+the fetch is usable. The ThreadPoolExecutor and the
per-row exception boundary stay intact. The offline default path must remain byte-identical to Phase 5.

Purpose: deliver the end-to-end BED-03 capability while preserving monotonicity (Pitfall 4) and the
no-crash guarantee (AC4). This is the last slice; after it, a keyed run measures real Lighthouse scores
and a keyless run degrades gracefully with an identical-to-before output.
Output: edited lead_analyzer/pipeline.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-optional-pagespeed-rate-limiting/06-RESEARCH.md

<read_first>
- lead_analyzer/pipeline.py — analyze_row (verdict list with scoring.DIM3_PLACEHOLDER at index 2) and run() (ThreadPoolExecutor fan-out, per-row boundary). This is the ONLY file edited.
- lead_analyzer/analyzers/performance.py (Plan 03) — analyze(fr, soup, ps_result).
- lead_analyzer/clients/pagespeed.py (Plan 04) — PageSpeedClient.from_config / is_available / score.
- lead_analyzer/scoring.py — bedarf() is already monotonic; DO NOT change bands. DIM3_PLACEHOLDER stays defined (other tests may reference it) — only its USE in analyze_row is replaced.
- 06-RESEARCH.md Pattern 1 (degradation contract wiring) and Pitfall 4/5 (monotonicity, call final_url, skip when not usable).
- tests/test_pipeline_bedarf.py::test_no_pagespeed_flag (Plan 01) and the existing pipeline/scoring regression tests.
</read_first>

<interfaces>
# Wiring shape (06-RESEARCH.md Pattern 1):
# run(): ps_client = PageSpeedClient.from_config(config)   # None if disabled/no key
#        pass ps_client into analyze_row(...)
# analyze_row(record, url_col, config, ps_client=None):
#   ps_result = None
#   if ps_client is not None and ps_client.is_available() and fr.ok and fr.html:
#       ps_result = ps_client.score(fr.final_url or fr.url)   # PsResult | None, never raises
#   verdicts[2] = performance.analyze(fr, soup, ps_result)    # replaces DIM3_PLACEHOLDER

PageSpeedClient.from_config(config) -> PageSpeedClient | None
PageSpeedClient.is_available() -> bool
PageSpeedClient.score(url) -> PsResult | None   # never raises
performance.analyze(fr, soup, ps_result=None) -> DimensionVerdict   # dim=3
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: thread the PSI client through run() and analyze_row</name>
  <files>lead_analyzer/pipeline.py</files>
  <behavior>
    - import performance from .analyzers; import PageSpeedClient from .clients.pagespeed.
    - analyze_row signature gains `ps_client=None` (keyword, default None → backward compatible with
      existing direct-call tests).
    - In analyze_row, after `soup = ...`:
        ps_result = None
        if ps_client is not None and ps_client.is_available() and fr.ok and fr.html:
            ps_result = ps_client.score(fr.final_url or fr.url)
      Replace the `scoring.DIM3_PLACEHOLDER,` entry (Dim 3) in the verdict list with
        `performance.analyze(fr, soup, ps_result),`.
    - The empty-URL early-return path and the except-boundary path are UNCHANGED (no fetch → no PSI).
    - run(): build `ps_client = PageSpeedClient.from_config(config)` ONCE before the pool; pass it into
      each submit: `pool.submit(analyze_row, r, url_col, config, ps_client)`. The client's semaphore +
      budget are shared across all threads (single instance). Everything else in run() unchanged.
    - SINGLE-CLIENT INVARIANT (PERF-02/AC8): from_config is called EXACTLY ONCE per run(), never inside
      analyze_row or the per-row loop. test_one_client_per_run (Plan 01) enforces this — it must turn GREEN
      with this wiring. Do NOT construct the client per row.
  </behavior>
  <action>
    Edit lead_analyzer/pipeline.py: add the two imports; add `ps_client=None` to analyze_row; insert the
    gated ps_result block; swap DIM3_PLACEHOLDER for performance.analyze(fr, soup, ps_result) at the Dim-3
    position; build the shared client once in run() and pass it through pool.submit. Update the module/function
    docstrings to say Dim 3 is now the real performance analyzer (viewport + optional PSI), no longer a placeholder.
    Keep the per-row try/except boundary and the ThreadPoolExecutor structure exactly as-is. German comments.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_pipeline_bedarf.py::test_no_pagespeed_flag tests/test_pipeline_bedarf.py::test_one_client_per_run -x -q && .venv/bin/python -m pytest tests/test_pipeline_bedarf.py -x -q</automated>
  </verify>
  <done>analyze_row accepts ps_client; Dim 3 uses performance.analyze; run() builds one shared client; test_no_pagespeed_flag AND test_one_client_per_run green (from_config called exactly once per run); per-row boundary unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: full-suite regression — offline output byte-identical (monotonicity)</name>
  <files>lead_analyzer/pipeline.py</files>
  <behavior>
    Confirm the offline default path is non-regressing:
    - With no PAGESPEED_API_KEY (default test environment), from_config returns None → ps_client None →
      ps_result None for every row → performance.analyze returns viewport-based ok/gap.
    - For the existing fixtures (reachable, viewport-present sites), Dim 3 = ok = 0 gap-points = identical
      to DIM3_PLACEHOLDER → scoring.bedarf unchanged → all existing pipeline/scoring tests stay green.
    - If a fixture site lacks a viewport-meta and a pre-existing test asserted a specific Bedarf that the new
      Dim-3 `gap` would change, that is a REAL behavior surfacing (BED-03). MECHANICAL JUSTIFICATION GATE
      (not judgment): before editing ANY assertion, grep the offending fixture/sample HTML to confirm it has
      NO viewport-meta — e.g. `grep -iL 'meta name="viewport"' <fixture.html>` (prints the file ONLY if the
      tag is absent) or `grep -ic 'meta name="viewport"' <fixture.html>` must return 0. ONLY if the grep
      confirms genuine absence may you update that ONE assertion, with an inline comment citing BED-03 and the
      grep result. NEVER weaken the analyzer, NEVER change scoring bands, NEVER edit an assertion for a
      viewport-PRESENT fixture (that would be a real wiring bug — fix pipeline.py instead). Document the grep
      output and the change in the SUMMARY.
  </behavior>
  <action>
    Run the FULL suite. If any non-Phase-6 test regresses, diagnose via the MECHANICAL grep gate in <behavior>:
    (a) grep the fixture HTML for `meta name="viewport"`; if the grep proves the tag is ABSENT, the row is
    correctly a Dim-3 gap — update that single assertion, cite BED-03 + the grep result; or (b) the fixture HAS
    a viewport (grep finds it) → it is a real wiring bug, fix pipeline.py (never scoring bands, never analyzer
    thresholds). Do NOT touch scoring.py bands. Record the grep output and any single justified assertion
    update in the SUMMARY.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest -q</automated>
  </verify>
  <done>Full suite green (≥177 existing + all new Phase-6 tests). Offline default output unchanged for viewport-present fixtures; any single assertion change is justified by a genuine no-viewport fixture and cited as BED-03 in the SUMMARY. Scoring bands untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-row fetch → PSI call | Only usable fetches (fr.ok and fr.html) trigger a PSI call; failures stay in the boundary |
| PSI client → per-row boundary | A None result must flow as "not attempted", never as an exception or a slow score |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-12 | Denial of service | PSI call on unreachable/blocked hosts wasting budget | mitigate | Gate ps_result on fr.ok and fr.html (skip dead/blocked rows — Dim 1 already drives those) |
| T-06-13 | Tampering (monotonicity break) | replacing placeholder shifts existing Bedarf | mitigate | viewport-present default → Dim 3 ok (0 pts) = identical G; scoring bands unchanged; full-suite regression gate |
| T-06-14 | Denial of service | one PSI failure aborting the whole run | mitigate | score() returns None; per-row try/except boundary unchanged; run() never sees a PSI exception |
| T-06-15 | Denial of service (budget-cap defeat) | future per-row client construction defeating the per-run budget/semaphore | mitigate | from_config called exactly once per run (shared instance); test_one_client_per_run asserts single construction, failing any per-row refactor |
</threat_model>

<verification>
- `.venv/bin/python -m pytest -q` fully green (existing 177 + all new Phase-6 tests).
- Offline default: a viewport-present reachable fixture yields the same Bedarf as before the swap.
- A (mocked) PSI 429/timeout in an integration-style test never aborts the run and never scores a site severe.
- run() constructs exactly one PageSpeedClient; the semaphore/budget are shared (single instance) — enforced by test_one_client_per_run (from_config called once per run, not per row; guards PERF-02/AC8 against per-row refactors).
</verification>

<success_criteria>
- Dim 3 in analyze_row is performance.analyze(fr, soup, ps_result); DIM3_PLACEHOLDER no longer used in the verdict list.
- One shared PSI client per run via from_config; ps_result gated on enabled+available+usable-fetch.
- Full suite green; offline output byte-identical for viewport-present fixtures; scoring bands untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/06-optional-pagespeed-rate-limiting/06-05-SUMMARY.md`
</output>
